# Evidence Notes: draft-ietf-intarea-schc-protocol-numbers-02

## analysis-gemini
Verdicts: {'conceptual': 'High', 'transition': 'Easy', 'adaptation': 'Trivial'}

### Executive assessment

SCHC Architecture -06 can naturally express draft-ietf-intarea-schc-protocol-numbers-02. This is not a coincidence of vocabulary: -06's Appendix A.2.3 is written about this draft by name, and it supplies the architectural reading of all three requested codepoints in a single sentence — the EtherType, IP Protocol Number, and UDP Port Number each "serves as the Discriminator: it indicates that the frame or packet carries a SCHC Datagram, and the Dispatcher uses it, together with lower-layer context (e.g., addresses or ports), to route the Datagram to the appropriate Instance."

The principal conceptual mapping is therefore **codepoint → Discriminator, consumed by the Dispatcher of the receiving Endpoint, in combination with lower-layer context**. The draft's own packet picture (Figure 1: RuleID, Compression Residue, Payload) is -06's Datagram Format with a trailing Payload, and needs no reinterpretation. The draft's Section 4 requirement that the datagram length be carried explicitly in the Compression Residue when the 802 header does not supply it is a legitimate profile-level constraint on Rule content, of exactly the kind -06 Section 5 anticipates.

The principal migration difficulty is the draft's Section 1.2 sentence "the use of SCHC to compress the transported protocol, as well as the SCHC session (called instance) to use, are implicit." That sentence treats "session" and "instance" as synonyms. In -06 they are distinct: an Instance is a logical component of an Endpoint that executes SCHC operations and owns a Context and an Instance Configuration, whereas a Session is a communication among two or more Instances sharing a Context. The draft's sentence is true of both under the LoRaWAN topology it describes, precisely because that topology forces a one-to-one correspondence — which is why the conflation is invisible there and breaks the moment the draft extends the argument to Ethernet, where an Endpoint may host several Instances. Migrating the sentence requires saying which of the two the Ethertype signals (neither: the Ethertype signals that SCHC is present; Instance and Session selection is the SCHC WG's chartered follow-on work). This is a decomposition, not a change of technical intent, and per the grading rules it does not by itself lower conceptual equivalence.

An Architecture gap does exist, and it is small. The draft's Section 6.3 floats an IANA registry that structures the RuleID: the top nibble of a 1-byte RuleID would select a "domain of use" such as aviation, and the value 15 would escape to a 2-byte RuleID. Two things are being asked of the RuleID here that -06 never grants or denies. First, that RuleID values carry meaning outside the Context in which they were minted — an IANA registry is a global administrative scope. Second, that a prefix of the RuleID be decodable, and select the RuleID's own length, before any Context is bound to the Datagram. -06 comes close: Section 4.2.5.1 already accepts that "a profile may define a fixed RuleID size and specify that RuleID values in a particular range are followed by a Control Header having a known format," and that "those framing semantics are independent of the C/D or F/R Rule selected by the RuleID." But -06 never defines RuleID in its Terminology section and never states its uniqueness scope, so the reader has no basis on which to judge the registry proposal. Closing the gap is additive: define RuleID, state that it is Context-scoped rather than Domain-scoped, and state that its size must be determinable before parsing. No existing -06 text changes meaning.

Because the adaptation need is Trivial, `schc-architecture-edits.md` accompanies this file. Because conceptual equivalence is High and transition difficulty is Easy, `terminology-migration.diff` accompanies it as well, and it is complete: the migration was performed end-to-end without any location requiring wording that does not exist in -06 or in the accepted edits.

### Architectural risk points

Not present.

### Needed modifications to the draft under study

| # | Section / location in draft | Current concept or text | Proposed change | Category | Rationale |
|---|---|---|---|---|---|
| 1 | Section 6.3, IANA SCHC Ethertype Registry | "A registry of SCHC RuleIDs for SCHC as an Ethertype may be needed... The top half of 1-14 assigned to different domains of use, like for aviation." | State that a RuleID identifies a Rule within a Context and is not made globally unique by such a registry; recast the registry as reserving RuleID ranges whose interpretation is agreed across the Domains that use the SCHC Ethertype, so that a receiving Endpoint can determine the RuleID size before interpreting any Context-dependent portion of the Datagram. Keep "domains of use" in lower case and distinguish it from an -06 Domain. | **REQUIRED FOR CONCEPTUAL ALIGNMENT** | The draft's Section 6.3 assumes a RuleID scope that its own Sections 3 and 4 contradict and that -06 does not define. The draft must take a position. This is the one place where migration changes an assumption rather than a word. See R1 and R5. |
| 2 | Section 1.2, first paragraph | "the SCHC session (called instance) to use, are implicit... there can be only one session... it is necessary to signal both the use of SCHC and the SCHC session to be used." | Separate Instance from Session. State that on a classical LPWAN link a single Instance and a single Session exist because the MAC-layer peers are preconfigured, that on Ethernet an Endpoint may host several Instances and take part in several Sessions, and that the Ethertype supplies the Discriminator indicating SCHC while Instance/Session selection remains the SCHC WG's chartered work. | **REQUIRED FOR CONCEPTUAL ALIGNMENT** | The draft fuses two concepts that -06 keeps distinct, and the fusion is exactly what fails when the draft extends its argument from LoRa to Ethernet. The one-to-one correspondence is a property of the topology, not of SCHC. See the Challenged mappings entry and A.2.1 Tables 1 and 2 of -06. |
| 3 | Section 3, final paragraph | "The RuleID may be statically configured per [RFC8724], or may be negotiated within a protocol as in IKE [ikev2-diet-esp]." | Add that the IKEv2 exchange provisions the Context binding out of band, before SCHC operation begins, and does not constitute negotiation between the compressing and decompressing Instances. | **REQUIRED FOR TERMINOLOGY MIGRATION** | -06 Section 1 keeps "no negotiation... between the compressing and decompressing entities" as a foundation while allowing Contexts to be "fetched on demand". The draft's behavior already complies; only the formulation leaves the reader unable to tell. See R4. |
| 4 | Section 1.2 | "The MAC-Layer endpoints are preconfigured" | Rewrite as "The MAC-layer peers are preconfigured". Do **not** translate to -06's Endpoint. | **REQUIRED FOR TERMINOLOGY MIGRATION** | -06's Endpoint is a logical SCHC entity, not a link-layer peer. Retaining "endpoint" here would collide with -06's term and assert something false. See R6. |
| 5 | Section 1.2 | "When extended to Ethernet and more powerful endpoint" | "When extended to Ethernet and to more capable Endpoints" — here the -06 Endpoint *is* meant. | **REQUIRED FOR TERMINOLOGY MIGRATION** | The same word is used in two senses two sentences apart. This occurrence does denote a SCHC entity; the previous one does not. |
| 6 | Section 3, "The SCHC compressed header with payload is shown below" and Figure 1 caption "SCHC Packet" | Draft's own name for the wire unit | Use "SCHC Datagram" for RuleID + Compression Residue, followed by the Payload. Retitle Figure 1 accordingly and bracket the Datagram in the figure. | **REQUIRED FOR TERMINOLOGY MIGRATION** | -06 Section 4.2.5 defines Datagram as "a Rule Identifier (RuleID) and the result of the SCHC operation... It may be followed by a Payload" — precisely Figure 1. -06 reserves "SCHC Packet" for RFC 8724's compressed-header unit (Section 4.2.2.1), so the draft's usage would be ambiguous. |
| 7 | Section 3, "An implementation should have a table of source IP address and RuleID size." | Flat receiver-side implementation structure | Frame as dispatch information: the IP Protocol Number is the Discriminator; the Dispatcher of the receiving Endpoint uses it together with the source IP address to select the Instance, and hence the Context and the RuleID size that apply. Keep the prefix-format recommendation. | **REQUIRED FOR TERMINOLOGY MIGRATION** | -06 Section 4.2.2.4: "Datagrams are routed to the appropriate Instance by the Dispatcher using the Discriminator and admission rules based on information provided in the Instance Configuration." A.2.3 states the codepoint "serves as the Discriminator". Same behavior, -06's decomposition. |
| 8 | Section 4, "The use of SCHC as an Ethertype is similar to that as in Section 3" | Ethertype's architectural role is left implicit | State that the Ethertype is the Discriminator and that the Dispatcher uses it with lower-layer context, typically the source MAC address, to select the Instance. | **REQUIRED FOR TERMINOLOGY MIGRATION** | -06 A.2.3, verbatim, for this codepoint. |
| 9 | Section 3, "The size of the SCHC RuleID is variable as described in [RFC8724]." | Variability stated without the receiver-side prerequisite | Add that the size must be known to the receiving Instance before the Datagram is parsed. | **REQUIRED FOR TERMINOLOGY MIGRATION** | Makes explicit the two-stage receive path the draft already assumes, and connects to Architecture edit 3. See R2. |
| 10 | Section 4, after the datagram-length MUST | No mention of the Control Header | Add a sentence noting that where a deployment defines a Control Header per Section 4.2.5.1 of the Architecture, its presence, placement and format must be known before the Datagram is parsed. Do **not** relax the existing MUST. | **OPTIONAL CLARIFICATION** | -06 A.2.3 Figures 14 and 15 illustrate a Control Header for exactly this SCHC-over-Ethernet case. The draft's residue-based choice remains valid; the note prevents a reader from concluding that the residue is the only option. Normative force is unchanged. |
| 11 | Section 1, "New IETF work has started with the SCHC WH" | Typographical | "the SCHC WG" | **EDITORIAL** | Typo. |
| 12 | Section 1, "This approach brakes down when dealing with Diet ESP" | Typographical | "breaks down" | **EDITORIAL** | Typo. |
| 13 | Section 1.2, "the Ethertype defined in this document will be used to signal SCHC as the upper* layer protocol" | Stray character | Remove the asterisk. | **EDITORIAL** | Typo. |
| 14 | Section 1.2, "leaving only destination address (32 bytes, source address calcuated from content)" | "calcuated" | "calculated" | **EDITORIAL** | Typo. (The "32 bytes" figure is a separate substantive question outside the scope of this review.) |
| 15 | References, `[schc-architecture]` | Cites `draft-ietf-schc-architecture-01`, 6 October 2023 | Cite `draft-ietf-schc-architecture-06`, 6 July 2026. | **EDITORIAL** | The draft cites a revision five versions behind the one it is being aligned to. No terminology in -01 is relied upon elsewhere in the draft. |

### Needed modifications to SCHC Architecture -06

| # | Architecture section | Architecture concept | Current text or definition | Proposed change | Category | Rationale |
|---|---|---|---|---|---|---|
| A1 | Section 3, Terminology | RuleID | **No entry exists.** RuleID appears only inside the definitions of *Rule* ("A structured description, identified by a RuleID...") and *Datagram* ("a Rule Identifier (RuleID) and the result of the SCHC operation") | Add a Terminology entry for RuleID that states what it identifies and in what scope it is unique, and that a profile may constrain RuleID allocation without extending that scope. | **ARCHITECTURE GAP** | The draft's Section 6.3 proposes an IANA registry of RuleID values. Whether that is architecturally coherent depends entirely on RuleID scope, which -06 never states. Additive: no existing definition changes meaning. |
| A2 | Section 4.1.2, last paragraph | Identifier scope | "Identifiers for Instances, Contexts, and Sessions are unique within the scope of their Domain." | Add a following sentence stating that the RuleID is not a Domain-scoped identifier — it identifies a Rule within a Context — and that a deployment needing to select an Instance or Context from information in the Datagram uses a Discriminator or a Control Header rather than relying on RuleID uniqueness. | **ARCHITECTURE GAP** | The existing sentence enumerates three identifiers and silently omits the fourth and most visible one, inviting the reader to assume RuleID shares the Domain scope. Purely additive; the quoted sentence is untouched and its meaning is unchanged. |
| A3 | Section 4.2.5.1 | RuleID size determinability | "The information needed to locate and decode the Control Header must be known before that information is used. For example, a profile may define a fixed RuleID size..." — the requirement is stated for the Control Header, not for the RuleID itself | Add a sentence requiring that the RuleID size be determinable by the receiving Instance before the Datagram is parsed, and listing the ways this may be achieved (fixed by a profile; derived from the Instance Configuration selected by the Discriminator; self-delimiting). | **ARCHITECTURE GAP** | The draft resolves RuleID size two different ways (source-address table in Section 3; self-describing top nibble in Section 6.3) and -06 gives neither a stated footing. Additive: it makes explicit a prerequisite -06's own Section 4.2.5.1 already relies on. |
| A4 | Appendix A.2.3 | Nested SCHC | The appendix covers SCHC directly over Ethernet, IPv6 and UDP, but shows no case where SCHC appears at two positions in one packet | Consider adding a worked deployment model for SCHC nested inside a security envelope (SCHC as IP Next Header, and SCHC as the Next Header within ESP), showing two Instances on one Endpoint distinguished by Stratum. | **OPTIONAL CLARIFICATION** | *Not a gap.* -06's existing concepts (multiple Instances, Stratum, Dispatcher) already express this, as the Concept mapping table records. The absence is of a worked example, and -06 Appendix B.2 already lists additional deployment models as future work. Recorded here so it is not mistaken for the gap in R3. **This row does not affect the Architecture adaptation verdict.** |

### Final migration assessment

- **Can the draft be migrated without changing technical behavior?** **Mostly.** Sections 3, 4, 5, 6.1 and 6.2 migrate with no behavioral change whatsoever; the requested codepoints, the IANA tables, the packet layout and the sole normative MUST are all untouched. Two locations change an assumption rather than a word. Section 1.2's "session (called instance)" must be split, which does not alter what any implementation does but does alter what the paragraph asserts about SCHC in general. Section 6.3 must state that its proposed registry does not make RuleIDs globally unique — a position the draft has not taken, and the only place in the migration where a technical assumption is resolved rather than restated.
- **Can the migration be performed mechanically?** **Mostly.** The three judgment calls (RuleID scope in Section 6.3; the session/instance split in Section 1.2; recognising that "endpoint" and "domain" are false friends in Sections 1.2 and 6.3) are each made once and then applied without further discretion. Everything downstream — Datagram naming, Discriminator framing of the three codepoints, the dispatch-information rewording — is substitution. The accompanying `terminology-migration.diff` is complete and contains no `<<BLOCKED>>` marker, which is the evidence supporting the Easy verdict.
- **Does the draft expose a SCHC Architecture -06 gap?** **Yes.** -06 does not define the RuleID in its Terminology section and does not state its uniqueness scope. Section 4.1.2 scopes Instance, Context and Session identifiers to the Domain and passes over the RuleID in silence; Section 4.2.5.1 requires the Control Header to be locatable before parsing but says nothing about the RuleID's own size. A reader of -06 cannot determine whether an IANA registry of RuleID values, as Section 6.3 of the draft proposes, is architecturally coherent.
- **Is the gap required for this draft or merely useful generally?** **Required for this draft, and useful generally.** Section 6.3 cannot be migrated to -06 terminology without an answer to the scope question, so the gap is squarely on this draft's critical path. But the question is not peculiar to it: any specification that carries a SCHC Datagram over a shared medium, or that reserves RuleID values across deployments, needs the same answer, and -06's Section 4.2.5.1 already leans on the unstated RuleID-size prerequisite that edit A3 supplies. The three edits are additive clarifications that would improve -06 independently of this draft, which is why the adaptation need is Trivial rather than Medium.
- **What is the single most important migration issue?** The RuleID scope question raised by Section 6.3. Everything else in this draft either maps Directly onto -06 or was, in the case of the three codepoints, written into -06's Appendix A.2.3 in advance. Section 6.3 alone asks -06 for something it has not defined — an identifier scope in which a RuleID prefix can mean "aviation" to a receiver that shares no Context with the sender — and it does so in a section the draft itself marks unresolved. Resolving it in the Context-scoped direction, and recasting the registry as a cross-Domain range reservation rather than a global uniqueness claim, is both the one substantive conceptual edit the draft needs and the one thing -06 must say for the edit to be sayable.
