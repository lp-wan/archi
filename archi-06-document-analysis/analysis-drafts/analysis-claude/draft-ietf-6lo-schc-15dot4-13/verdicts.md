# Architectural alignment review: draft-ietf-6lo-schc-15dot4-13

## Verdicts
- Conceptual equivalence: **High**
- Transition difficulty: **Easy**
- SCHC Architecture adaptation need: **Trivial**

## Grade calibration

| Criterion | Verdict | Why not one grade higher? | Why not one grade lower? |
|---|---|---|---|
| Conceptual equivalence | High | Not Very High because a small family of concepts inherited from architecture **-05** requires explicit interpretation, not mere renaming: (i) "SCHC Data end point" must be decomposed into the -06 triple Instance + Context + Session; (ii) the "SCHC Instance ID" field is defined in the draft as identifying "the session between SCHC end points ... using a common SoR", conflating what -06 separates into Instance ID, Session ID, and Context ID, so a semantics-preserving choice must be made; (iii) PRO/Mesh-Under RuleID reuse across disjoint peer pairs is expressible only under a Session-scoped reading of -06's undefined term "compatible" Contexts; (iv) SRO hop-by-hop decompression/recompression requires deciding whether forwarding routers are participants of a multi-Instance Session or of per-hop Sessions. | Not Medium because no concept needs reframing or additional constraints beyond natural -06 use: Stratum, Discriminator, Dispatcher, Control Header, SoR, Rule/RuleID, Datagram, and the multi-Strata stacking are Direct matches; the draft's compressed Control Header carrying a SCHC-compressed Instance ID is nearly verbatim the -06 Appendix A.1 example (same FID `SCHC.insid`, same MSB(7)/LSB rule); TRO's storage model is exactly -06 Appendix A.3's compatible-partial-Contexts pattern; the technical model is unchanged everywhere. |
| Transition difficulty | Easy | Not Very Easy because it is not a pure find-and-replace: Section 3.2.2/3.2.3 must be rewritten as prose around Instances/Contexts; the "SCHC Instance ID" definition sentence (4.1.2) needs a semantics-preserving rewrite; and the word "endpoint/end point" is overloaded in the draft (the -05 architectural concept, a plain-English communicating node, and figure identifiers E1/E2), so each occurrence needs per-case resolution. | Not Medium because all edits are local rewording at identifiable locations (§3.1.1, §3.2, §3.4–3.5, §4.1.1–4.1.3, §5–5.2, §6.1–6.2, references, figure captions); once the four mapping decisions are made they apply repeatably; no section's technical content, frame format, or normative requirement is restructured. The complete migration diff (`terminology-migration.diff`) was produced without any blocked location, which is direct evidence for Easy. |
| SCHC Architecture adaptation need | Trivial | Not None because two notions the draft relies on are implicit or undefined in -06: (1) the draft requires a node to know its Dev/App role *per peer* ("for each node it communicates with", §6.1/§6.2, citing the architecture), while -06 defines Role as an Instance Configuration parameter, i.e., per Instance — role-per-Session is only implied by one sentence in 4.2.1.1; (2) PRO/Mesh-Under Single-Instance networks let the same RuleID identify *different* Rules for different peer pairs, which is expressible only if -06's undefined notion of "compatible" Contexts (6.2) is defined as scoped to the Rules actually used within a Session. | Not Medium because both gaps close through purely additive clarifications (a definition of Context compatibility; a note that Role may be determined per Session): no existing -06 definition or normative statement changes meaning, and no architectural concept is added, removed, or re-scoped. |

## Executive assessment

SCHC Architecture -06 **can naturally express** draft-ietf-6lo-schc-15dot4-13. The draft was written against architecture **-05** and already builds on architecture vocabulary (SCHC Stratum, Discriminator, SCHC Control Header, SCHC Datagram, SoR); its wire formats, storage requirements, and the SRO/TRO/PRO/Mesh-Under modes carry over unchanged.

The **principal conceptual mapping** is the decomposition of the -05 term family the draft still uses: a node's "SCHC Data end point" maps to an -06 **Instance with its Context (SoR)**; the group of end points that "share a Rule" (the E1/E2 identifiers in Figures 9/11/14/16) maps to **Instances sharing a Context, communicating within a Session**; "Single/Multiple-end point networks" map to deployments where Endpoints host **one vs. several Instances**; the "SCHC Control Header end point" reduces to the profile-defined **SoR used for Control Header C/D**, exactly as -06 4.2.5.1 requires (decodable before any Context-dependent part). The draft's compressed Control Header design coincides with -06 Appendix A.1 almost verbatim.

The **principal migration difficulty** is bounded and local: rewriting Section 3.2 around Instances, disambiguating the overloaded word "end point", and rewording the "SCHC Instance ID" definition, which currently conflates the -06 notions of Instance, Session, and Context identifier.

An **Architecture gap exists but is Trivial**: -06 should (1) state that an Instance's Role may be determined per Session, and (2) define Context "compatibility" as scoped to the Rules used within a Session. Both are additive clarifications proposed in `schc-architecture-edits.md`.

## Native conceptual model

| Native draft concept | Meaning in the draft | Owner / logical location | Scope | Identifier or selector | Cardinality and relationships | Notes |
|---|---|---|---|---|---|---|
| SCHC-Lo network | A 6LoWPAN network where SCHC is used for header C/D | The network deployment | Network / administrative | — (implicit; the L2 network) | 1 network : N nodes | New term defined by the draft (§2.3) |
| 6LN / host | Constrained node generating/receiving packets; runs SCHC HC at layer 2.5 (and possibly higher Strata) | Physical node | Node | L2/IPv6 addresses | 1 node : 1..N SCHC Data end points; 1 node : 1..N Strata | Hosts store Rules for their own communications |
| 6LR | Intermediate router. SRO: stores **all** Rules, may decompress/recompress in flight. TRO/PRO/Mesh-Under: stores no Rules beyond its own traffic; in PRO it reads/edits pointer-designated residue bits | Physical node | Node | L2/IPv6 addresses | SRO: 1 router : all Rules; TRO/PRO: 0 Rules for transit | PRO 6LR manipulates compressed data without Rules |
| 6LBR | Border router. SRO/TRO: stores all Rules in the network; PRO: stores Rules for communications with external nodes | Physical node | Node | Addresses | 1 per network (typically) | Also RPL root in TRO |
| Mesh-Under forwarder ("m") | L2-level forwarder | Physical node | Link/mesh | Mesh Header addresses | No Rules | Transparent to SCHC |
| External host | Internet peer participating in SCHC C/D at some Stratum | Remote node | Global | IPv6 address | Peer of a 6LN via 6LBR-held Rules | e.g., Host E, Host F |
| SCHC Stratum | Portion of the stack where a SCHC C/D function is applied (layer 2.5 for IPv6; above UDP for CoAP; inner/outer CoAP for OSCORE) | Per node, per layer placement | Node-local placement; peers agree per Stratum | Position in stack; in TPS: Protocol ID / Port / Option number in the Control Header | 1 node : 1..N Strata | Term taken from the architecture; Contexts at different Strata may come from different provisioning domains |
| SCHC Data end point | Entity/state grouping on a node owning **one SoR** for SCHC Datagram C/D | Per node | Groups of peer nodes share it ("two end points that share a Rule") | SCHC Instance ID (in the uncompressed Control Header); E1/E2 in figures | 1 node : 1 (Single-end point nets) or N (Multiple-end point nets); 1 end point : 1 SoR | -05 architecture term; central migration object |
| SCHC Control Header end point | Entity on a node owning the (single) SoR for **Control Header** C/D | Per node | Network-wide common | — (implicit) | Always exactly 1 per node; its SoR has 1 implicit Rule (Single-end point) or several (Multiple-end point) | -05 term; not defined in -06 |
| Single-end point network | Network where all SCHC nodes have one SCHC Data end point (one SoR) | Network classification | Network | — | Control Header compressed to 0 bits | §3.2.2 |
| Multiple-end point network | Network where ≥ some nodes have several SCHC Data end points | Network classification | Network | Control Header (>0 bits) discriminates | 1 SoR per end point | §3.2.3 |
| Rule / RuleID | RFC 8724 Rule; in this document used for header compression only | Stored on hosts/routers per mode | SRO/TRO Single-end point: RuleID+Rule **unique network-wide**; PRO/Mesh-Under: RuleID reusable across disjoint peer pairs | RuleID (1–16 bits recommended for SCHC Data; 0–8 bits for Control Header) | 1 Rule : 1 pair of peers (typically); 1 RuleID : N pairs allowed in PRO/Mesh-Under; routers may share all | Rules "intended for the communication between" node pairs |
| SoR | Set of Rules of a SCHC Data end point (or of the Control Header end point) | Per end point per node | Shared with peers; in SRO all nodes MAY share the same SoR | — | 1 end point : 1 SoR | |
| SCHC Instance ID | Unsigned integer in the uncompressed Control Header; "identifies the session between SCHC end points in two or more peer nodes using a common SoR" | Carried on the wire (compressed); provisioned | SCHC-Lo network (assignment unstated) | Itself | 1 value : 1 group of end points sharing a SoR | 1–8 bits recommended; conflates instance/session |
| SCHC Dispatch / SCHC Pointer Dispatch | 6LoWPAN Dispatch Type values (01000100 Page 0/1; 01000101 Page 0) announcing SCHC framing; the draft calls this the Discriminator | IANA / frame | Global (IANA) | Bit pattern | 1 : network-wide meaning | §3.2.1, §4.1.1, §4.3, §9 |
| SCHC Datagram | Control Header (possibly 0 bits) + SCHC Data + user payload | On the wire | Per packet | — | — | Cited to the architecture (§4.1) |
| SCHC Control Header | Determines the SCHC Data end point for decompression; itself SCHC-compressed (RuleID + residue); uncompressed content = SCHC Instance ID (or Protocol ID / Port / Option number in the TPS) | On the wire | Network-wide decodable (single Control-Header SoR) | Its RuleID | 0 bits when a single end point exists | §4.1.2, §5.1–5.2 |
| SCHC Data | RuleID + compression residue of the packet header | On the wire | Interpreted via the selected end point's SoR | RuleID | — | = RFC 8724 Compressed Header |
| User payload | Payload of the original packet, unaffected by the SCHC Stratum | On the wire | — | — | — | §4.1.4 |
| SRO | Straightforward Route-Over: every router stores all Rules; may decompress/recompress in flight | Mode of the network | Network | SCHC Dispatch | All routers : all Rules | RuleID network-wide unique (Single-end point) |
| TRO | Tunneled RPL Route-Over: IPv6-in-IPv6 to/from root; only communicating nodes (and 6LBR) store Rules | Mode | Network | Page 1 + SCHC Dispatch | 6LR : 0 transit Rules; 6LBR : all Rules | Updates RFC 9008/8138 |
| PRO | Pointer-based Route-Over: PRO Header (CID/DCI, Control Header, Bit Pointer, HLM, Address Residue Length) lets Rule-less routers read the destination residue and modify Hop Limit/ECN | Mode | Network | SCHC Pointer Dispatch | Routers : 0 Rules; 6LBR : external Rules | Requires destination-fields-first Rule design; forbids match-mapping for destination |
| Mesh-Under | RFC 4944 L2 mesh forwarding of SCHC Datagrams | Mode | Link mesh | Mesh Header; originator address selects the Data end point | Forwarders : 0 Rules | Broadcast/multicast supported |
| TPS | Transition protocol stack: 6LoWPAN-compressed IPv6 carrying SCHC-compressed UDP/CoAP (1–3 Strata) | Nodes | Network | SCHC Internet Protocol Number (NH); Control Header per Stratum; sender IPv6 address as Discriminator in mixed networks | 1–3 Strata | §5 |
| Prefix context (DCI) | RFC 6282-style prefix context identifying the IPv6 destination prefix in PRO | Network provisioning | Network | 4-bit DCI | Shared with 6LoWPAN-HC nodes | Distinct from SCHC Context |
| Dev/App roles | RFC 8724 roles used so one Rule serves both directions; in mesh, each C/D entity must know its role **per peer** | Per node, per peer pair | Peer pair | — | 1 node : possibly different roles toward different peers | §6.1, §6.2; cites the architecture |
| 6LoWPAN F/R | Fragmentation is 6LoWPAN/6lo (RFC 4944/8930/8931), not SCHC F/R | Adaptation layer | Link/path | Frag headers | — | SCHC F/R not used; no SCHC per-session F/R state |

## Native architectural model

The draft defines how SCHC header compression replaces (or complements) 6LoWPAN header compression in IEEE 802.15.4 networks. Its protocol stack keeps 6LoWPAN fragmentation and inserts a SCHC header-compression layer at layer 2.5; additional SCHC compression may apply at higher layers (UDP, CoAP, CoAP inner/outer under OSCORE), each such placement being a "SCHC Stratum". A network operating this way is a "SCHC-Lo network".

The unit on the wire is a SCHC Datagram, introduced by a 6LoWPAN Dispatch Type (the SCHC Dispatch, or the SCHC Pointer Dispatch in PRO), which the draft identifies as the Discriminator. The Datagram consists of a SCHC Control Header, the SCHC Data (RuleID plus compression residue of the original header), and the user payload, plus padding to an octet boundary.

State is organized around "SCHC Data end points": a node owns one SoR per SCHC Data end point. In "Single-end point networks" every SCHC node has exactly one Data end point (one SoR), and the Control Header compresses to zero bits; in "Multiple-end point networks" some nodes have several Data end points, and a non-empty compressed Control Header selects the one to use. The uncompressed Control Header carries a single field, the "SCHC Instance ID", described as identifying "the session between SCHC end points in two or more peer nodes using a common SoR". The Control Header is itself SCHC-compressed using a separate, network-wide-common SoR owned by the node's single "SCHC Control Header end point"; in Single-end point networks that SoR contains one implicit Rule.

Rules are defined per pair of communicating nodes: "RuleID 1 is intended for communication between Host A and Host B". Which nodes must store which Rules depends on the multihop mode. In SRO, every router stores all Rules in use in the network and may decompress and recompress packets in flight; RuleIDs (and the Rules they identify) must be unique network-wide in Single-end point networks, and all nodes MAY share one SoR. In TRO, packets are tunneled through the RPL root; only the communicating nodes and the 6LBR store Rules, and transit 6LRs store none — each node holds exactly the subset of the network's Rules that concerns it. In PRO, a pointer-bearing header lets Rule-less routers locate and read the IPv6 destination-address residue and decrement the Hop Limit residue inside the compressed header; RuleIDs may then be reused by different, disjoint pairs of peers to denote different Rules, because routing on the destination residue guarantees that a Datagram only reaches a node holding the right binding. Mesh-Under forwards at L2; the originator address in the Mesh Header may substitute for the Control Header in selecting the Data end point.

PRO constrains Rule design: destination-address field descriptors must precede source-address descriptors (an update to RFC 8724 field ordering), one Rule per direction is required, and match-mapping must not be used for destination fields, so that pointer-designated residue bits have position-stable, Context-free meaning for routers.

The TPS (transition protocol stack) keeps RFC 6282 compression for IPv6 and applies SCHC above it to UDP/CoAP, in one, two, or three Strata. There, the uncompressed Control Header content is the identifier of the protocol whose header the Stratum compresses (Protocol ID 17, Port 5683, Option 9), fully elided for TPS-only devices whose per-Stratum SoR holds a single Rule; in mixed networks the sender's IPv6 address serves as a Discriminator to know whether the peer elides the Control Header.

Management is deliberately out of scope: how Contexts are provisioned, how network-wide RuleID uniqueness is ensured, and how the DCI prefix context is distributed are all external. The draft notes that Contexts at different Strata may come from different provisioning domains. Fragmentation of oversized SCHC Datagrams uses 6LoWPAN/6lo mechanisms, not SCHC F/R, so the draft has no SCHC fragmentation state. For bidirectional Rule sharing the RFC 8724 Dev/App roles are used; in mesh topologies each C/D entity must know its role for each node it communicates with, so a node's role can differ per peer. Security relies on L2 mechanisms; integrity protection of SCHC-compressed headers is required, and PRO's pointer fields are identified as a new manipulation surface.

## Concept mapping

(Final table, after Pass 3 corrections.)

| Native draft concept | Meaning in the draft | SCHC -06 mapping | Match type | Scope alignment | Cardinality alignment | Semantic differences | Notes |
|---|---|---|---|---|---|---|---|
| SCHC-Lo network | 6LoWPAN network using SCHC HC | Deployment term; **not** mapped to Domain (kept as a profile term) | Profile-specific | Network vs. Context-sharing group: different axes | n/a | A SCHC-Lo network is a topology/L2 construct; only the SRO shared-SoR case coincides with a single Domain | Revised in Pass 3 (initially Domain) |
| 6LN / host / external host | SCHC-processing node | Physical node hosting an -06 **Endpoint** with its Instance(s) | Direct | Node = equipment; Endpoint = logical entity on it | 1 node : 1..N Endpoints/Instances — matches | -06 Endpoint is logical, node is physical; migration keeps "node" for the machine | |
| 6LR / 6LBR (SRO) | Router storing all Rules, decompressing/recompressing in flight | Endpoint + Instance whose Context is the network-common Context; participates in Sessions with the communicating hosts (or per-hop Sessions) | Composite | Node-attached state = Instance Context: aligned | 1 router Instance : all Sessions of the network | Session membership of a forwarder is not spelled out by either document | See risk point 4 |
| 6LR (TRO / Mesh-Under) | Rule-less transit forwarder | Not a SCHC entity; below/outside the Stratum | Direct (non-SCHC) | — | — | None | |
| 6LR (PRO) | Rule-less router that reads/edits pointer-designated residue bits | On-path actor operating on **profile-defined framing** (PRO Header + designated bit ranges); not an Instance | Profile-specific | The router holds no Context — consistent with not being an Instance | — | -06 has no concept of Context-free partial processing of a Datagram; the draft self-contains it as framing plus Rule-design constraints | See risk point 1 |
| SCHC Data end point | Per-node owner of one SoR for Datagram C/D | **Instance** (with its own Context/SoR); the cross-node group sharing the SoR = Instances sharing a **Context**, communicating in a **Session** | Composite | -05 "end point" is node-local; Instance is Endpoint-local: aligned | 1 node : 1..N end points ↔ 1 Endpoint : 1..N Instances; 1 end point : 1 SoR ↔ 1 Instance : 1 Context — exact | None once decomposed; the composite preserves the original semantics | Central mapping |
| SCHC Control Header end point | Per-node owner of the single Control-Header SoR | The **Rules needed to decode the Control Header**, profile-defined framing per -06 4.2.5.1 (pre-Context decodable) | Composite / Profile-specific | Network-wide common SoR ↔ profile-fixed framing rules: aligned | Exactly 1 per node ↔ profile constraint | -06 does not reify a processing entity for the Control Header; none is needed | The "end point" wording disappears in migration |
| Single-end point network | All SCHC nodes have one Data end point | Deployment where each Endpoint hosts a **single Instance** (single Context); Control Header elided | Composite (profile constraint) | Network-level classification: -06 naturally permits it | 1:1 Endpoint:Instance is an allowed -06 case (cf. A.2.1) | None | Rename to "Single-Instance network" |
| Multiple-end point network | Some nodes have several Data end points | Endpoints hosting **multiple Instances**; Dispatcher + Discriminator/Control Header select the Instance | Composite | Matches -06 4.2.2.4 exactly | 1:N supported natively | None | Rename to "Multiple-Instance network" |
| SCHC Instance ID | Field "identifying the session between SCHC end points ... using a common SoR" | Discriminator carried in the Control Header identifying the **Instance, and thus the Context** (cf. -06 A.1: `SCHC.insid` "used to identify the Instance and the Context that apply to the Datagram") | Direct (after rewording; initially Partial) | Uniqueness within the deployment ↔ -06: identifiers unique within their Domain | 1 value : 1 group of Instances sharing a Context — expressible | Draft text conflates Instance/Session/Context selection; -06 permits any of the three as Control-Header multiplexing content, so one must be chosen | REQUIRED FOR CONCEPTUAL ALIGNMENT in the draft |
| SCHC Stratum | Layer placement of SCHC processing | **Stratum** | Direct | Identical | 1 node : N Strata ↔ multiple Endpoints/Instances per equipment "to serve different ... strata" | None | -06 term matches |
| SCHC Dispatch / Pointer Dispatch | 6LoWPAN Dispatch announcing SCHC framing; "the Discriminator" | **Discriminator** used by the **Dispatcher** | Direct | Global (IANA) value + lower-layer context: exactly -06 A.2.3's EtherType pattern | — | None | |
| Sender IPv6 address / Mesh originator address as selector | Selects the Data end point / whether the Control Header is elided | Discriminator ("combination of several criteria") | Direct | Peer-derived criteria are contemplated by -06 | — | None | |
| Rule / RuleID / SoR | RFC 8724 Rules for header C/D | **Rule / RuleID / SoR** (within a Context) | Direct | RuleID scope: per-Context in -06; draft adds mode-dependent profile constraints (see below) | 1 SoR : 1 Context — aligned | Network-wide RuleID uniqueness (SRO/TRO) is a legitimate profile *constraint*; cross-pair RuleID reuse (PRO/Mesh-Under) needs the Session-scoped compatibility clarification | |
| Per-pair Rules, partial storage (TRO) | Each node stores only the Rules concerning it; 6LBR stores all | **Compatible partial Contexts** (-06 6.2 / Appendix A.3 — same picture: root holds Rules 1–3, leaves their subsets) | Direct | Aligned | Aligned | None | Strongest single alignment |
| RuleID reuse across disjoint pairs (PRO/Mesh-Under) | Same RuleID → different Rules for different pairs; routing disambiguates | Per-node Contexts that are compatible **for each Session** but divergent elsewhere | Partial → Direct once File-2 Edit 2 is accepted | RuleID meaning stays per-Context; cross-Context collisions tolerated by delivery guarantees | Aligned | -06's "compatible" is undefined; the strict identical-Context reading of Session would forbid this | Drives the Trivial adaptation verdict |
| SCHC Datagram | Control Header + SCHC Data + user payload | **Datagram** (RuleID + rule-dependent fields, may include payload; Control Header before/after RuleID per -06 Figures 7–8) | Direct | Identical framing | — | None (draft places the Control Header before the RuleID, an option -06 shows explicitly) | |
| SCHC Control Header | Selects the end point; itself SCHC-compressed; TPS variant carries Protocol ID/Port/Option | **Control Header** (4.2.5.1): multiplexing (Instance/Context id), metadata (compressed-protocol identity), itself SCHC-compressible with pre-available Rules | Direct | Pre-Context decodability: both documents state the same requirement | — | None; draft Figure 18 ≈ -06 Figure 10 | |
| SCHC Data | RuleID + compression residue | The RuleID + residue portion of the Datagram (RFC 8724 Compressed Header) | Direct | — | — | -06 defines no term "SCHC Data"; draft-local term kept, citation moved to RFC 8724 | |
| User payload | Untouched payload | Payload following the SCHC operation result | Direct | — | — | None | |
| SRO | Hop-by-hop C/D at routers, shared Rules | One Domain sharing a common Context; multi-Instance Sessions (or per-hop Sessions) including router Instances | Partial | Sessions with >2 Instances are explicitly allowed by -06 | Aligned | Whether transit routers are Session participants is an interpretation the draft leaves implicit | See risk point 4 |
| TRO | Tunnel via root; ends-only Rules | End-to-end Sessions between the communicating Instances; transit transparent | Direct/Composite | Aligned | Aligned | None | |
| TPS | SCHC over 6282-compressed IPv6, 1–3 Strata | Strata above IP; Protocol-Number Discriminator (cf. -06 A.2.3); per-Stratum Instances/Contexts | Direct/Composite | Aligned | Aligned | None | |
| Prefix context (DCI) | 6282-style destination-prefix context | Lower-layer (6LoWPAN) mechanism, **not** a SCHC Context | Direct (non-SCHC) | Network scope, non-SCHC | — | Naming collision with SCHC "Context" only; migration keeps "prefix context" clearly distinct | |
| Dev/App roles per peer | Role known per communicating peer | Instance **Role** (Instance Configuration), determined **per Session** | Partial → Direct once File-2 Edit 1 is accepted | Role per peer ↔ role per Session | 1 Instance : N Sessions with possibly different roles | -06 defines Role per Instance; per-Session role only implied ("all Instances of a Session are aware of their role") | Drives the Trivial adaptation verdict |
| 6LoWPAN F/R instead of SCHC F/R | SCHC F/R not used | Instance Configuration Manifest listing C/D only (no F/R) | Direct | — | No SoV needed (C/D is stateless) | None | |
| Padding | Octet alignment | Profile framing | Direct | — | — | None | |

## Scope and cardinality comparison

| Relationship or identifier | Draft model | Architecture -06 model | Alignment | Consequence |
|---|---|---|---|---|
| Ownership of Context | SoR owned per SCHC Data end point per node; the *same* logical Rules replicated on peers (and on all routers in SRO) | Context owned per Instance; the same Context shared by two or more Instances (Session/Domain) | Aligned | Direct migration: "end point's SoR" → "Instance's Context (SoR)" |
| Ownership of Set of Rules | 1 Data end point : 1 SoR; plus one network-common SoR for the Control Header | 1 Context : 1 SoR; Control-Header Rules pre-available per profile | Aligned | None |
| Ownership of Set of Variables | Not applicable — the draft uses no SCHC F/R and defines no per-session runtime SCHC state | SoV is per Session | Not applicable | The SoV concept is simply unused by this profile |
| Endpoint ↔ SCHC Instance | 1 node : 1..N SCHC Data end points | 1 Endpoint : 1..N Instances | Aligned (exact) | The Single/Multiple-end point classification becomes Single/Multiple-Instance |
| SCHC Instance ↔ Session | One end point serves all peers sharing its SoR (e.g., A's E1 serves pairs A–B, A–C, A–E) | One Instance may handle multiple Sessions with the same Context (-06 A.2.1, Table 1) | Aligned, with the Role caveat | Requires per-Session Role (File 2, Edit 1) |
| Sharing of Context between Sessions/Instances | SRO: all nodes MAY share one SoR; TRO: subsets of one logical rule set; PRO/Mesh-Under: per-node SoRs compatible only pairwise | Same Context across Sessions allowed; compatible partial Contexts allowed (6.2, A.3) | Aligned for SRO/TRO; Partial for PRO/Mesh-Under | Needs the Session-scoped definition of compatibility (File 2, Edit 2) |
| RuleID scope | Per SoR per node; network-wide unique in SRO/TRO Single-Instance networks (normative); reusable across disjoint pairs in PRO/Mesh-Under | Unique within a Context; Instance/Context/Session identifiers unique within their Domain | Aligned; the network-wide uniqueness is a profile constraint -06 permits | None beyond File 2 Edit 2 |
| Discriminator scope | 6LoWPAN Dispatch (global IANA value) + sender IPv6 / Mesh originator address (network scope) | Deployment-defined; "combination of several criteria"; -06 A.2.3 uses exactly the presence-indicator + lower-layer-context pattern | Aligned | None |
| Control Header processing scope | Single network-wide Control-Header SoR; decodable before any Context-dependent field; 0 bits when a single Instance exists | Presence/placement/format profile-defined; must be decodable before Context-dependent portions; its Rules pre-available | Aligned | None |
| Domain membership and boundaries | Not used; "provisioning domains" mentioned for Contexts at different Strata; no claim that a SCHC-Lo network is one Domain | Domain = Instances sharing a common set of Contexts; Domain Manager, Context Repository | Aligned by non-use (profile need not invoke Domain); SRO shared-SoR networks naturally form one Domain | Migration adds only an informative Domain remark; PRO networks are not asserted to be a single Domain |
| SCHC Instance ID uniqueness | Unstated assignment; implicitly unique within the SCHC-Lo network | Identifiers unique within their Domain | Aligned under the deployment≈Domain-scope reading; assignment out of scope in both | Ambiguity preserved (management out of scope in the draft) |

## Challenged mappings

| Concept | Initial mapping | Challenge | Final mapping | Reason for change |
|---|---|---|---|---|
| SCHC-Lo network | Composite → Domain | In PRO/Mesh-Under, nodes hold mutually divergent Contexts (same RuleID, different Rules) and do not all "share a common set of Contexts"; equating the network with a Domain holds only in the SRO shared-SoR case and depends on that example topology | Profile-specific (deployment term kept; Domain mentioned only informatively for SRO) | Domain is a Context-sharing grouping, not a topology; forcing the equation would misstate PRO networks |
| RuleID reuse across pairs (PRO/Mesh-Under) | Direct (via -06 6.2 "subset of the SoR") | -06 6.2 covers *subset* Contexts; PRO produces *overlap-divergent* Contexts (A: RuleID2=A–C rule; B: RuleID2=D–B rule) between nodes that DO share a Session on other Rules; strict reading of "share a common Context" fails | Partial; becomes Direct only under the Session-scoped compatibility definition (File 2, Edit 2) | The undefined term "compatible" must be pinned down for the mapping to be natural |
| SCHC Instance ID | Direct → -06 Instance ID (A.1) | The draft's own definition says the field identifies "the session ... using a common SoR" — under -06 that is a Session ID or Context ID, while the field name says Instance; carrying it forward untouched would silently overload -06's "Instance" | Direct after a REQUIRED FOR CONCEPTUAL ALIGNMENT rewording (field identifies the Instance, and thus the Context; peers sharing the Context use the same value) | The mechanism is native (-06 A.1 is nearly identical); only the defining sentence conflates concepts |
| Dev/App role | Direct → Instance Configuration Role | With one Instance per node and per-pair Rules, a node's role differs per peer; -06 defines Role as an Instance-level configuration parameter; the per-Session reading is implied by a single sentence and deferred by B.2 | Partial; Direct once File 2 Edit 1 is accepted | Per-Session role is required by the draft (§6.1/§6.2) but only implicit in -06 |
| PRO 6LR | Composite (Dispatcher-like on-path component) | The Dispatcher routes Datagrams within an Endpoint; a PRO 6LR is on the network path, holds no Context, and *modifies residue bits* — no -06 component has that purpose; calling it a Dispatcher would be a misleading natural-use violation | Profile-specific: an on-path actor operating on profile-defined framing (PRO Header + Rule-design constraints), outside the -06 component model | Natural-use rule; -06 need not model it for the draft to be expressible, since the draft fully self-defines the behavior |

## Architectural risk points

- **Risk:** PRO lets Context-free on-path nodes read and *modify* bits inside the compression residue (Hop Limit, ECN) of a SCHC Datagram.
  **Why it matters:** -06's implicit model is that Datagram content between Instances is interpreted only via the shared Context; PRO's safety rests entirely on profile-imposed Rule-design constraints (destination-fields-first ordering, no match-mapping for destination fields, one Rule per direction). If a future architecture revision asserted Datagram immutability or Context-only interpretation, PRO would conflict.
  **Consequence for migration:** The migrated text must present PRO 6LR behavior as operation on profile-defined framing, not as SCHC processing by an Instance; no architecture change is required today, but the framing must be kept explicit.

- **Risk:** In PRO/Mesh-Under Single-Instance networks, correctness of RuleID interpretation depends on delivery guarantees (routing on the destination residue / Mesh originator address), not on Context agreement.
  **Why it matters:** A misrouted, multicast, or spoofed Datagram carrying a reused RuleID would be decompressed with the wrong Rule with no architectural detection; -06's Context-consistency section does not contemplate deliberate cross-Session RuleID collisions.
  **Consequence for migration:** Expressible once compatibility is defined as Session-scoped (File 2, Edit 2); the residual risk is a property of the draft's design, to be kept visible in its Security Considerations, not a mapping failure.

- **Risk:** The "SCHC Instance ID" field name vs. its "identifies the session" definition.
  **Why it matters:** -06 distinguishes Instance ID, Session ID, and Context ID, all usable as Control-Header multiplexing content; keeping the conflated definition would import an ambiguity into -06 vocabulary and could mislead implementers about what must be unique where.
  **Consequence for migration:** One REQUIRED FOR CONCEPTUAL ALIGNMENT rewording (kept as Instance-selecting, matching -06 A.1); no wire-format change.

- **Risk:** SRO's in-flight decompression/recompression leaves Session membership of routers implicit.
  **Why it matters:** Whether a forwarding router is a participant of the A↔B Session or terminates per-hop Sessions affects, in principle, where per-Session state (SoV, Role) would live. Because this profile uses stateless C/D only and no SCHC F/R, the choice has no operational consequence here, but it would surface if SCHC F/R were later allowed end-to-end.
  **Consequence for migration:** No text change strictly required; an optional clarification in the draft avoids future ambiguity.

- **Risk:** Assignment and uniqueness scope of the SCHC Instance ID are unstated (management is out of scope).
  **Why it matters:** -06 requires Instance/Context/Session identifiers to be unique within their Domain; the draft neither names a Domain nor an assigner.
  **Consequence for migration:** Preserved as an explicit provisioning assumption (already out of scope in the draft); no mapping failure, but the migrated text should not accidentally claim Domain-wide management that the draft does not provide.

## Needed modifications to the draft under study

| # | Section / location in draft | Current concept or text | Proposed change | Category | Rationale |
|---|---|---|---|---|---|
| 1 | §4.1.2 | "This field is an unsigned integer that identifies the session between SCHC end points in two or more peer nodes using a common SoR." | "This field is an unsigned integer that identifies the SCHC Instance, and thus the Context, that applies to the SCHC Data. The Instances of two or more peer nodes that share that Context (and thus a common SoR) within a Session use the same SCHC Instance ID value." | REQUIRED FOR CONCEPTUAL ALIGNMENT | The current sentence conflates -06 Instance, Session, and Context selection; the mechanism is -06 A.1's Instance-ID multiplexing and must be stated as such |
| 2 | §3.2.2, §3.2.3 | Single/Multiple-end point networks defined via "SCHC Data end point" and "SCHC Control Header end point" (concepts absent from -06) | Redefine as Single-Instance / Multiple-Instance networks: each node hosts one vs. several SCHC Instances, each Instance with its Context (SoR); the Control-Header SoR is stated directly, without an "end point" entity | REQUIRED FOR CONCEPTUAL ALIGNMENT | The -05 end-point entity model must be re-expressed through the -06 Instance/Context decomposition; behavior unchanged |
| 3 | §3.2 intro | Concept list citing "SCHC Control Header end point", "SCHC Data end point" to the architecture | Cite the -06 concepts actually used: Endpoint, Instance, Session, Context, SoR, Stratum, Discriminator | REQUIRED FOR TERMINOLOGY MIGRATION | -06 does not define the -05 terms |
| 4 | §3.5.1–3.5.4 text and Figures 8–16 (captions, "Nodes \| End point" columns, E1/E2 narrative) | "SCHC Data end point called E1", "SCHC Datagram Instance called E1", "same end point identifier ... for two end points that share a Rule", "Single-/Multiple-end point network" | "SCHC Instance (and its Context) denoted E1"; "the same identifier ... for the Instances of different nodes that share a Context"; rename network classes; column header "Instance" | REQUIRED FOR TERMINOLOGY MIGRATION | Uniform application of the mapping; also fixes the internal drift ("SCHC Datagram Instance") |
| 5 | §3.4, §3.5, §3.5.2, App. A.2/A.3 | "endpoints"/"endpoint" used for communicating nodes ("between those two endpoints", "involve the 6LR itself as an endpoint") | "nodes" / "as a communicating node" | REQUIRED FOR TERMINOLOGY MIGRATION | Avoids collision with -06 "Endpoint" (a logical SCHC entity) |
| 6 | §4.1.2, §4.1.3 | "determines the SCHC Data end point to be used to decompress"; "compressed by using a SCHC Data end point" | "identifies the SCHC Instance, and thus the Context, to be used to decompress"; "compressed by a SCHC Instance" | REQUIRED FOR TERMINOLOGY MIGRATION | Same mapping |
| 7 | §5 | "SCHC may also need a Discriminator to identify the SoR to be used for header decompression" | "...a Discriminator, used by the Dispatcher to select the appropriate SCHC Instance, and thus the Context (with its SoR)..." | REQUIRED FOR TERMINOLOGY MIGRATION | -06 Discriminator selects the Instance; the SoR follows from it |
| 8 | §5.1, §5.2 (incl. Figures 30–36 labels and notes) | "SCHC Stratum end point", "SoR of the SCHC Stratum end point", figure label "SCHC Stratum Header" | "the Instance at that SCHC Stratum", "SoR of the Instance at that SCHC Stratum", label "SCHC Control Header" | REQUIRED FOR TERMINOLOGY MIGRATION | Removes the -05 end-point term and the draft-internal "Stratum Header"/"Control Header" inconsistency |
| 9 | §6.1, §6.2 | "each SCHC C/D entity needs to know its role (Dev or App) ... for each node it communicates with" | "each SCHC Instance needs to know its role (Dev or App), for each Session it participates in, ..." | REQUIRED FOR TERMINOLOGY MIGRATION | Maps to the -06 Role (per Session, per File 2 Edit 1); the per-peer requirement is unchanged |
| 10 | §3.5.4 | "determine the SCHC Data end point needed to decompress ... based on the packet's originator address" | "determine the SCHC Instance (and thus the Context) ... ; in that case, the originator address is used as a Discriminator" | REQUIRED FOR TERMINOLOGY MIGRATION | Names the -06 mechanism already in use |
| 11 | §12.1 and all citations | Normative reference `draft-ietf-schc-architecture-05`; inline cites `[draft-ietf-schc-architecture]`, `[draft-ietf-schc-arch]` | Reference -06; unify inline citations to `[I-D.ietf-schc-architecture]` | REQUIRED FOR TERMINOLOGY MIGRATION | The migration target is -06 |
| 12 | §3.5.1 | "all nodes in the SCHC-Lo network MAY share the same SoR" | Add: "(i.e., their Instances share a common Context and form a single Domain)" | OPTIONAL CLARIFICATION | Makes the natural Domain mapping of the SRO shared-SoR case explicit |
| 13 | §3.5.3 | PRO 6LR behavior (reads/edits residue without Rules) | Add a note that PRO 6LRs operate on the PRO Header framing and the pointer-designated bit ranges, and are not SCHC Instances | OPTIONAL CLARIFICATION | Prevents misreading PRO routers as SCHC processing entities |
| 14 | §3.5.3 / §3.5.4 | RuleID reuse across disjoint pairs | Add a pointer to Context compatibility per Session (Section 6.2 of the architecture, as clarified) | OPTIONAL CLARIFICATION | Anchors the reuse feature to the architectural notion that legitimizes it |
| 15 | §8 | 6LoWPAN F/R used instead of SCHC F/R | Optionally note that, in -06 terms, the Instance Configuration Manifest includes C/D only | OPTIONAL CLARIFICATION | Explicit but derivable |
| 16 | §4.1.1 | "a SCHC Sratum header ('SCHC Hdr' in Figure 17...))" | "a SCHC Control Header ('SCHC Hdr' in Figure 17...)" — fixes the typo, the wrong term, and the doubled parenthesis | EDITORIAL | "Sratum" is a typo and the field in question is the Control Header |
| 17 | §3.5 | "Straightfoward Route-Over" | "Straightforward Route-Over" | EDITORIAL | Typo |
| 18 | §3.5.4, Figure 16 narrative | "the Rules that need to be stored by the nodes in PRO" | "...in Mesh-Under" | EDITORIAL | The paragraph describes Figure 16, a Mesh-Under example |
| 19 | §3.2.3 first line | "In Multiple-endpoint networks" | Consistent class name | EDITORIAL | Internal consistency (subsumed by #2/#4) |

## Needed modifications to SCHC Architecture -06

| # | Architecture section | Architecture concept | Current text or definition | Proposed change | Category (ARCHITECTURE GAP / OPTIONAL CLARIFICATION) | Rationale |
|---|---|---|---|---|---|---|
| 1 | 4.2.1.1 (Instance Configuration — Role) | Role of the Instance | Role is listed as an Instance Configuration parameter ("Role of the Instance (e.g., Upside or Downside...)"); per-Session role is only implied ("all Instances of a Session are aware of their role") and the Role model is deferred to future work (B.2) | Add a paragraph stating that when an Instance participates in several Sessions, its Role may differ per Session, and the role-determination method must make each Instance aware of its role in each Session | ARCHITECTURE GAP (Trivial) | draft-ietf-6lo-schc-15dot4 §6.1/§6.2 requires a node to know its Dev/App role *per peer* while hosting a single Instance; without the clarification, expressing this needs one Instance per peer, which the draft's Single-Instance model contradicts |
| 2 | 6.2 (Context consistency) | Compatible Contexts / "share a common Context" | "...it is possible for one or more Instances to have only a subset of the SoR, as long as the Contexts of the Instances participating in a given session remain compatible." — "compatible" is undefined and only the subset case is illustrated | Define compatibility as scoped to the Session: Contexts are compatible for a Session when every Rule used within it is bound to the same RuleID and definition in each participant's Context; RuleIDs may denote unrelated Rules in Contexts of Instances that share no Session, provided the deployment guarantees delivery only to holders of the Rule used | ARCHITECTURE GAP (Trivial) | Required to naturally express PRO/Mesh-Under RuleID reuse across disjoint peer pairs (draft §3.5.3/§3.5.4), where per-node Contexts are overlap-divergent, not subsets |
| 3 | 4.2.5 (Datagram Format) / Terminology | Datagram vs. Payload | "It may be followed by a Payload." alongside "A Datagram may be an unfragmented SCHC Packet" (a SCHC Packet includes the payload) | Optionally state explicitly that the Payload, when present, is part of the Datagram | OPTIONAL CLARIFICATION (not a gap) | The draft's figures bracket the user payload inside the SCHC Datagram; the -06 text already supports this reading via the SCHC Packet equivalence — clarification only improves explicitness |

## Final migration assessment
- Can the draft be migrated without changing technical behavior? **Yes** — every change is terminological or a conceptual reformulation; no wire format, storage requirement, or normative behavior changes.
- Can the migration be performed mechanically? **Mostly** — four mapping decisions (end point → Instance/Context/Session; Instance ID semantics; network-class renaming; "endpoint" disambiguation) must be made once, after which application is repetitive; Section 3.2 needs rewritten prose rather than substitution.
- Does the draft expose a SCHC Architecture -06 gap? **Yes** — a Trivial one (two additive clarifications: per-Session Role; Session-scoped Context compatibility). See `schc-architecture-edits.md`.
- Is the gap required for this draft or merely useful generally? **Required for this draft** (per-peer Dev/App roles in mesh topologies, §6.1/§6.2; RuleID reuse across disjoint pairs, §3.5.3/§3.5.4), and also generally useful for any peer-to-peer or mesh SCHC deployment.
- What is the single most important migration issue? **Replacing the -05 "SCHC Data end point / SCHC Control Header end point" concept family with the -06 Instance/Context/Session decomposition — in particular the "SCHC Instance ID" field, whose current definition ("identifies the session ... using a common SoR") conflates what -06 separates and must be reworded to the Instance-selecting semantics of -06 Appendix A.1.**
