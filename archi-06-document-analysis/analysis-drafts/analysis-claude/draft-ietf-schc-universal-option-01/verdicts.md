# Architectural alignment review: draft-ietf-schc-universal-option-01

Sources:
- `SOURCE CONFIRMED: Options representation in SCHC YANG Data Models (draft-ietf-schc-universal-option-01) — 6 sections + Appendices A–F / 41 pages (2296 lines) — obtained from /Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-universal-option-01.txt`
- `SOURCE CONFIRMED: Static Context Header Compression (SCHC) Architecture (draft-ietf-schc-architecture-06) — 9 sections + Appendices A–B / 30 pages (1680 lines) — obtained from /Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt`

## Verdicts
- Conceptual equivalence: **Very High**
- Transition difficulty: **Easy**
- SCHC Architecture adaptation need: **Trivial**

## Grade calibration

| Criterion | Verdict | Why not one grade higher? | Why not one grade lower? |
|---|---|---|---|
| Conceptual equivalence | Very High | Highest grade | Not "High": no concept of the draft requires explicit reinterpretation. The only mapping that is not one-to-one — the draft's "SCHC endpoint / SCHC node" resolving to Endpoint + Instance (+ Session for the communicating pair) — is a natural composite renaming that preserves the draft's semantics exactly. space-id/option-id/entry-option-space are Rule-internal Data-Model structures, and -06 explicitly delegates Rule internals to the Data Model referenced by the Context. The residue-ordering constraint is an instance of -06's Context-consistency notion, merely not yet spelled out there. |
| Transition difficulty | Easy | Not "Very Easy": three locations require sentence-level local rewriting rather than word substitution — the Section 2.2 scenario narrative (S1/S2 must be framed as Endpoints hosting Instances that share a Context within a Session), and Sections 5.2 and 5.4.3, where the normative-adjacent pointer "should be documented in [I-D.ietf-lpwan-architecture]" must be redirected to the -06 Context-consistency text (assuming the Trivial architecture edit). These involve a one-to-two term resolution (endpoint → Endpoint vs. Instance), i.e., light architectural judgment at identifiable spots. | Not "Medium": the edits are few, localized, and repeatable; the entire technical core (YANG module, CBOR/SID/CORECONF appendices, compression examples) is untouched by the migration; no section requires restructuring; a complete migration diff was produced (File 3) without blocked locations. |
| SCHC Architecture adaptation need | Trivial | Not "None": -06 nowhere states that the order of Field Descriptors (entries) within a Rule is part of the shared Context and must be preserved by Context distribution/synchronization. The draft explicitly requires the architecture document to state this constraint (Sections 5.2 and 5.4.3), so without a small additive clarification the draft's normative pointer has no landing place in -06. | Not "Medium": closing the gap requires only one additive paragraph in Section 6.2 (Context consistency) making explicit a relationship already implied by "identical SoR" / "compatible Contexts". No existing -06 definition or normative statement changes meaning; no concept is added, removed, or re-scoped. |

## Executive assessment

SCHC Architecture -06 can naturally express draft-ietf-schc-universal-option-01. The draft is, architecturally, a Data-Model document: it extends the RFC 9363 YANG model with an `entry-option-space` list keyed by a protocol namespace (`space-id`) and the protocol's native option identifier (`option-id`), so that Rules can describe options unknown to the deployed implementation. Everything below the Rule boundary (FID, space-id, option-id, entries, TV/MO/CDA, SIDs, CBOR, CORECONF) sits at a granularity that -06 deliberately delegates to the Data Model named in the Context's metadata — a natural, defined use of the -06 Context concept, not an escape hatch.

The principal conceptual mapping is: the draft's "SCHC endpoint / SCHC node" (S1, S2) → an -06 Endpoint hosting an Instance, the communicating pair forming a Session; the draft's "Rules they share" → a shared Context (Set of Rules); the draft's "rule mngt" between endpoints → Context management/synchronization (Domain Manager function or an Instance-executed dynamic Context update, both of which -06 provides).

The principal migration difficulty is small: resolving the overloaded "endpoint" per occurrence and redirecting the two references to the obsolete [I-D.ietf-lpwan-architecture] to the -06 architecture.

An architecture gap exists but is Trivial: -06 must state explicitly, in its Context-consistency considerations, that entry order within a Rule is part of the Context and is preserved by Context distribution and synchronization (residue serialization depends on it). This is exactly the statement the draft asks the architecture to carry. The corresponding edit is proposed in `schc-architecture-edits.md`.

## Native conceptual model

| Native draft concept | Meaning in the draft | Owner / logical location | Scope | Identifier or selector | Cardinality and relationships | Notes |
|---|---|---|---|---|---|---|
| Device (A, B) | Host generating/consuming CoAP packets, possibly with newly defined or private options | End hosts | Node-local | None (CoAP-level identity only) | 1 Device attached to 1 adjacent SCHC node in the example; not normatively constrained | Not a SCHC entity; source of the "unknown option" problem |
| SCHC node / SCHC endpoint (S1, S2) | Entity on the path performing SCHC compression/decompression using Rules shared with its peer | Network nodes between the Devices | Peer-pair (shares Rules with its peer) | None explicit | 2 per shared rule set in all examples; N>2 not excluded | Draft uses "node", "endpoint", "end-point", and "implementation" interchangeably |
| SCHC implementation | The deployed SCHC software, including its built-in knowledge of FIDs / the Data Model | Per SCHC node | Node-local | — | Implicitly one per node | Central to the problem statement: the implementation predates newly defined options |
| Rule | Structured description of how to compress/decompress a packet; a list of entries | Stored identically on both SCHC endpoints | Shared between the peer endpoints | RuleID (rule-id-value + rule-id-length) | 1 Rule : N entries; the rule set is shared by both endpoints | RFC 8724 semantics assumed |
| Rule entry / Field Descriptor (FID, FL, FP, DI, TV, MO, CDA) | Matches and processes one header-field occurrence | Within a Rule | Rule | YANG key (field-id, field-position, direction-indicator) | Many per Rule; key unique within the Rule | RFC 9363 `entry` list |
| Field Identifier (FID) | SCHC-specific abstract identifier of a protocol field/option | Defined in the YANG Data Model (RFC 9363 / 8824-update) | Global (standardized identityrefs, SIDs) | identityref / SID | One predefined FID per known field/option; new options have none | The FID space is disconnected from the protocol option space — the core problem |
| Protocol option identifier | The option's native number from the protocol's registry (e.g., CoAP option 11 = Uri-Path) | Protocol specification / IANA | Global within the protocol | Option number | One per option type; the same number can mean different options in different protocols | E.g., option 8 = Location-Path in CoAP, Timestamp in TCP |
| space-id ("protocol space") | Identifies the protocol namespace an option-id belongs to | New YANG module; values "provided by the SCHC Working Group" | Global | identityref (space-type) / SID | One per protocol; many option-ids per space | New concept introduced by the draft |
| option-id | The option identifier within a space, as in the protocol's IANA registry | Rule entry in `entry-option-space` | Global within its space-id | uint32 | Entry key = (space-id, option-id, field-position, direction-indicator), unique within the Rule | Preserves the protocol's native numbering inside SCHC Rules |
| entry-option-space list | New list of Field Descriptors dedicated to protocol options | Per compression Rule (YANG augment of RFC 9363) | Rule | List key above | One list per Rule; many entries; coexists with the standard `entry` list | The draft's central technical artifact |
| Rule management ("rule mngt") | Derives new Rules (e.g., S1 deriving a more specific Rule) and communicates Rule descriptions so both endpoints share them | Spans the two SCHC endpoints (Figure 1) | Peer-pair | — | One management relationship per endpoint pair in the example | The performing entity is left implicit (ambiguity — see risk points) |
| Residue serialization order | The order in which entry residues appear in the SCHC packet; must be identical on both endpoints; standard `entry` fields MUST be serialized before `entry-option-space` fields | Property of the Rule as processed by both endpoints | Peer-pair (must match) | Implicit list order; "ordered-by user" required in a 9363 revision | Standard entries strictly before option-space entries | Draft states (twice) that this constraint should be documented in [I-D.ietf-lpwan-architecture] |
| YANG Data Model / SIDs / CBOR / CORECONF | Representation, global identification, serialization, and remote query of Rules | Rule store on each node; SIDs allocated globally per YANG module | Global (SIDs); node-local (data store) | SID; CORECONF instance identifiers | One SID per schema node; delta encoding is a serialization artifact | Management/representation machinery, extensively exercised in Appendices C–D |
| Predefined CoAP option FIDs (to deprecate) | Legacy per-option FIDs of RFC 9363 / 8824-update | YANG Data Model | Global | identityref | One per predefined option | Draft proposes deprecation with a migration path |
| SCHC compressed packet | RuleID + residues (residue order follows entry order) | On the S1–S2 link | Link/peer-pair | RuleID | One per compressed packet | Ordering convention is what makes the packet interpretable |

## Native architectural model

The draft addresses a Data-Model-level interoperability problem in SCHC compression of protocols with extensible options, using CoAP as the concrete case. In the current model (RFC 9363, RFC 8824), each CoAP option is abstracted into a SCHC-specific Field Identifier (FID) defined as a YANG identityref. The protocol's own option-number space and the SCHC FID space are disconnected: when a protocol defines a new option, or a deployment uses a private option, the deployed SCHC implementation has no FID for it and no standardized way to mint one or to communicate a minted one to its peer.

The operational setting is a path A — S1 — S2 — B: Devices A and B exchange CoAP traffic, and two SCHC nodes S1 and S2 compress and decompress that traffic using Rules they share. A rule-management function, drawn as spanning S1 and S2, can derive new Rules (e.g., S1 derives a more specific Rule matching a URI-Path) and must communicate the Rule description to the peer. For options with predefined FIDs this works today, because RFC 9363 supplies interoperable identityref values. For a new or private option, S1 can still parse the CoAP header (the option wire structure is self-describing) but has no identifier to put in the Rule that S2 would understand — the interoperability failure the draft sets out to fix.

The draft examines two families of solutions. The syntactic approach decomposes each on-the-wire option into three fields (option delta/number, length, value), each a generic FID. Option identifiers then remain in the CoAP numbering space carried as target values, so any option can be described, at the cost of tripled rule entries and degraded compression when option values must be sent (length information travels twice). The semantic approach — the one the draft proposes — keeps SCHC's field-abstraction efficiency but stores the protocol's native option identifier directly in the Rule, qualified by a protocol namespace ("protocol space") so that identical numbers in different protocols do not collide.

Technically, the proposal is a YANG augmentation of the RFC 9363 compression rule: a new list `entry-option-space` keyed by (space-id, option-id, field-position, direction-indicator), carrying the same descriptive leaves as the standard `entry` list (field-length, MO, CDA, target values, etc.). space-id values are to be provided by the SCHC Working Group; option-id carries the option number from the protocol's IANA registry. The standard `entry` list remains for non-option fields (IPv6, UDP, fixed CoAP header), because burdening every entry with a namespace would be wasteful.

Splitting a Rule's Field Descriptors across two YANG lists surfaces a latent constraint: residue serialization follows entry order, so both SCHC endpoints must process entries in the same order. The draft therefore requires that fields of the standard `entry` list be serialized before those of `entry-option-space`, that entry order not be changed when Rules are transmitted between endpoints, and that a revision of RFC 9363 mark the lists "ordered-by user". Twice, the draft states that this ordering constraint should be documented in the architecture document (referenced as [I-D.ietf-lpwan-architecture], the predecessor of the current SCHC architecture draft).

Around this core, the draft exercises considerable management machinery in its appendices: Rules are serialized in CBOR using YANG SIDs, transferred and queried via CORECONF, and the draft compares five representation strategies (RFC 9363 semantic, Universal Options, merged model, manually ordered SID allocation, syntactic) on rule size, query size, and compressed-packet size. SID allocation is a global, per-module identifier assignment; the draft shows that manual SID ordering materially affects CBOR delta encoding but nothing else.

Several aspects are left implicit. The draft never says who operates the rule-management function (a peer node, a central manager, an operator); the arrow in Figure 1 simply spans both endpoints. All examples are two-endpoint and unidirectional-pair topologies, but nothing normatively restricts the shared rule set to exactly two parties. The draft also implicitly assumes the rule-management channel is trusted (Rules installed on both sides are authoritative), and that identifier authorities (IANA for option numbers, the SCHC WG for space-ids, the SID registry for SIDs) guarantee global uniqueness. Finally, the draft's scenario of run-time Rule derivation departs from a purely static pre-provisioned context, without discussing how the derived Rule is validated or synchronized beyond "communicate this identifier to S2".

## Concept mapping

(Final table, after Pass 3 corrections.)

| Native draft concept | Meaning in the draft | SCHC -06 mapping | Match type | Scope alignment | Cardinality alignment | Semantic differences | Notes |
|---|---|---|---|---|---|---|---|
| Device (A, B) | Host generating/consuming CoAP traffic | Host running applications, outside the SCHC entities (as in -06 Figure 11/12 "Dev"/"App") | Direct | Aligned (node-local) | Aligned | None | Not a SCHC architectural entity in either document |
| SCHC node / SCHC endpoint (S1, S2) | On-path entity performing C/D with shared Rules | Endpoint hosting a (single, in the examples) Instance; the communicating pair forms a Session | Composite | Aligned: draft's peer-pair sharing = -06 Instances sharing a Context in a Session | Aligned: draft never asserts one-Instance-per-node as a constraint; -06's 1:N Endpoint:Instance is unconstrained by the draft | "Endpoint" in the draft conflates -06 Endpoint and Instance; resolved per occurrence | The composite preserves semantics fully |
| SCHC implementation | Deployed software + its FID/Data-Model knowledge | Endpoint/Instance implementation; predefined FIDs live in the Data Model referenced by the Context | Composite | Aligned (node-local) | Aligned | None | "Implementation cannot handle new options" = "Data Model lacks an identifier", cleanly expressible |
| Rules shared between endpoints | The common rule set enabling C/D | Context (Set of Rules + metadata) shared between the Instances of a Session/Domain | Direct | Aligned | Aligned: -06 allows 2..N Instances per Context, superset of draft usage | None | Core mapping |
| Rule / Rule entry (Field Descriptor) | RFC 8724 Rule and its FID/FL/FP/DI/TV/MO/CDA entries | -06 Rule (identified by RuleID); entry internals delegated to RFC 8724 and the Data Model | Direct | Aligned (Rule-internal) | Aligned | None | -06 deliberately does not model below-Rule granularity |
| RuleID | Selects the Rule; first element of the compressed packet | RuleID; the compressed packet = -06 Datagram (RuleID + residue) | Direct | Aligned: both scope RuleID to the shared rule set / Context | Aligned | None | — |
| FID | Abstract SCHC field identifier defined in the Data Model | Element of the Data Model (RFC 9363) named in the Context metadata | Direct | Aligned (global registry) | Aligned | None | — |
| space-id / option-id | Protocol namespace + native option number inside a Rule entry | Data-Model structure within a Rule; defined by the (revised) Data Model that the Context references | Profile-specific | Aligned: global identifier spaces, used Rule-internally | Aligned | None | Natural use: -06 states the Context "may specify … which Data Model, such as the one defined in [RFC9363]" — this draft revises exactly that Data Model |
| entry-option-space list | Second Field-Descriptor list per compression Rule | Data-Model structure within a Rule | Profile-specific | Aligned (Rule scope) | Aligned (1 Rule : 2 entry lists : N entries) | None | Below-architecture granularity by design |
| Rule management ("rule mngt") | Derivation of new Rules and communication of Rule descriptions so both endpoints share them | Context management: Domain Manager context provisioning/synchronization, and/or Instance-executed "dynamic Context update mechanisms" (-06 §4.2.1) | Composite | Aligned: -06 scopes Context management to the Domain, which covers the draft's peer-pair | Aligned | Draft leaves the managing entity unspecified; -06 offers two natural realizations | Ambiguity preserved, not silently resolved — see risk point 1 |
| Rule derivation by S1 | S1 derives a more specific Rule at run time | Dynamic Context update mechanism, permitted by -06 in "complex scenarios"; installation on both sides is Context synchronization | Direct | Aligned | Aligned | Must be framed as management-plane update, not in-band negotiation (-06 retains RFC 8724's no-negotiation foundation) | See risk point 1 |
| Residue serialization order / entry-order preservation | Both endpoints must process entries in the same order; order preserved when Rules are transmitted; standard entries before option-space entries | Context consistency (-06 §6.2) + Data Model ordering ("ordered-by user") | Partial → Direct once the Trivial clarification of `schc-architecture-edits.md` is applied | Draft: peer-pair; -06: Session/Domain-wide Context consistency — compatible (superset) | Aligned | -06 currently never states that entry order is part of the Context | The single ARCHITECTURE GAP (Trivial) |
| YANG SIDs / CBOR serialization / CORECONF queries | Rule representation, transfer, and remote query | Data Model + Domain-management functions (Context Repository, provisioning/synchronization); CORECONF is a deployment realization -06 itself cites (Appendix B.2) | Composite | Aligned (global SIDs; node-local stores) | Aligned | -06 leaves management-protocol specifics as future work, which is precisely where a Data-Model document like this draft plugs in | Not a gap: the architectural functions exist; only their protocol realization is deferred |
| Predefined CoAP option FIDs / deprecation | Legacy identifiers to be deprecated in a 9363 revision | Data-Model lifecycle matter | Profile-specific | Aligned | Aligned | None | No architectural content |
| Syntactic vs semantic compression | Two strategies for describing options in Rules | Alternative Rule contents under RFC 8724 mechanics; invisible at -06 level | Direct | Aligned | Aligned | None | Architecture-neutral design discussion |

## Scope and cardinality comparison

| Relationship or identifier | Draft model | Architecture -06 model | Alignment | Consequence |
|---|---|---|---|---|
| Ownership of Context | Rules are "shared" by the two SCHC endpoints; each stores the set | Context shared by two or more Instances; managed within a Domain (Domain Manager, Context Repository) | Aligned (draft is the 2-Instance case of -06's 2..N) | Direct rewording only |
| Ownership of Set of Rules | Same as above (the draft does not separate SoR from its metadata) | SoR is the Rule collection inside a Context; metadata (Data Model, parser) accompanies it | Aligned | The draft's YANG model is exactly -06's "Data Model" metadata; mapping strengthens under -06 |
| Ownership of Set of Variables | Not applicable — the draft defines no runtime/session variables (no F/R state discussed) | SoV is per-Session | Not applicable | None |
| Endpoint ↔ SCHC Instance | Not distinguished; one C/D entity per node in all examples | 1 Endpoint : N Instances | Aligned — draft's examples are the N=1 case; no normative one-Instance assumption anywhere in the draft | Migration resolves "endpoint" to Endpoint or Instance per occurrence |
| SCHC Instance ↔ Session | Implicit single communication relationship per endpoint pair | 1 Instance : N Sessions possible; Session = the communicating subset of a Domain | Aligned (draft is the single-Session case) | None |
| Sharing of Context between Sessions/Instances | One rule set per endpoint pair in examples; N-party sharing not excluded | Explicitly supported (one Context, many Sessions; -06 Appendix A.2.1 Tables 1–2) | Aligned | The draft's mechanism is indifferent to how many Instances share the Context |
| RuleID scope | Meaningful within the rule set shared by the peer endpoints | RuleID scoped to the Context; interpreted within a Session | Aligned | Direct |
| space-id scope | Global (values provided by the SCHC WG) | Not an -06 concept; global identifier inside the Data Model | Aligned (no conflict; -06 delegates Rule-internal identifier spaces to the Data Model) | None |
| option-id scope | Global within its space-id (protocol IANA registry) | Same delegation as above | Aligned | None |
| Entry key uniqueness | (fid, fp, di) and (space-id, option-id, fp, di) unique within one Rule | Rule internals delegated to RFC 8724 / Data Model | Aligned | None |
| Entry order within a Rule | Must be identical on both endpoints and preserved in transmission; standard entries before option-space entries | Not stated in -06; implied by "identical SoR" / "compatible Contexts" (§6.2) but never explicit | Gap (Trivial) | The one required -06 clarification; see `schc-architecture-edits.md` |
| Discriminator scope | Not applicable — the draft performs no Instance dispatch | Discriminator routes Datagrams to Instances within an Endpoint | Not applicable | None |
| Control Header processing scope | Not applicable — the draft defines no control information beyond RuleID + residue | Control Header optional, profile-defined | Not applicable | None |
| Domain membership and boundaries | Implicit single administrative relationship covering S1, S2, and the rule-management function | Domain = Instances sharing a common set of Contexts; Domain Manager manages them | Aligned (draft is a single-Domain case) | Migration may name the Domain explicitly for clarity (optional) |
| SID uniqueness scope | Global per YANG module (SID registry) | Not an -06 concept; management/Data-Model machinery | Aligned (no conflict) | None |

## Challenged mappings

| Concept | Initial mapping | Challenge | Final mapping | Reason for change |
|---|---|---|---|---|
| Residue serialization order / entry-order preservation | Profile-specific (handled entirely by the Data Model via "ordered-by user") | The draft does not merely solve the ordering in the Data Model — it states twice, as an interoperability requirement, that the constraint *should be documented in the architecture*. Classifying it as purely Data-Model-internal would hide the draft's explicit architectural dependency inside an extensibility mechanism, leaving the draft's pointer dangling after migration. -06's §6.2 ("identical SoR", "compatible Contexts") implies but never states order preservation. | Partial; ARCHITECTURE GAP (Trivial). Becomes Direct (Context consistency) once the §6.2 clarification is applied. | Natural-use rule: the mapping target must carry the concept explicitly; -06 currently does not. |
| Rule management ("rule mngt") | Direct (Domain Manager context synchronization) | Figure 1 draws management as spanning S1 and S2 directly, and §2.2 has S1 itself deriving a Rule and communicating it — the managing entity is unspecified. Forcing the mapping to "Domain Manager" alone would silently resolve an ambiguity and could misstate the draft's intent (peer-driven update is equally consistent with its text). -06 offers both realizations: Domain Manager provisioning/synchronization, and Instance-executed dynamic Context update mechanisms. | Composite (Domain Manager function and/or Instance dynamic Context update; the choice is deployment-specific and the draft's ambiguity is preserved) | Avoid an implicit management assumption; both -06 realizations preserve the draft's semantics without changing its technical model. |

All other Direct, Composite, and Profile-specific classifications survived the adversarial pass unchanged, including under multiple-Instance, multiple-Session, shared-Context, and multi-Domain deployments (the draft's mechanisms are Rule-internal and indifferent to those multiplicities).

## Architectural risk points

- **Risk:** Run-time Rule derivation vs. the static-Context foundation. The draft's scenario (S1 derives a new Rule and communicates it to S2) must be expressed as a management-plane Context update; -06 explicitly retains RFC 8724's assumptions that the Context is provisioned before use and that *no negotiation takes place between the compressing and decompressing entities*, while permitting dynamic Context update mechanisms.
  **Why it matters:** If the draft (or a successor) were read as requiring in-band Rule negotiation between the communicating Instances, it would collide with a stated foundation of -06 rather than a peripheral detail.
  **Consequence for migration:** The migrated text should frame Rule derivation and installation as Context management (Domain Manager or authorized dynamic update), which the current draft text supports; no technical change is needed, but the framing must be deliberate.

- **Risk:** Silent reordering of entries during Context synchronization. YANG lists are unordered unless marked "ordered-by user"; a management pipeline (CORECONF store, intermediate tooling) could legally reorder entries, changing residue interpretation without any visible Context mismatch.
  **Why it matters:** Both Instances would believe they hold "the same Context" (same entries, same keys) yet decompress incorrectly — a failure -06's current Context-consistency text does not warn against.
  **Consequence for migration:** Requires the Trivial -06 clarification (entry order is part of the Context) plus the draft's own "ordered-by user" requirement on the Data Model; with both in place the risk is closed.

- **Risk:** Overloaded "endpoint". The draft's "SCHC endpoint" denotes at different moments the -06 Endpoint (the hosting entity), the Instance (the executing entity), and the implementation.
  **Why it matters:** A mechanical one-word replacement would either imply one Instance per Endpoint (an unstated 1:1 constraint -06 does not have) or misattribute per-Session processing obligations (entry ordering binds the *Instances of a Session*, not physical nodes).
  **Consequence for migration:** Each occurrence must be resolved individually (done in File 3); this is the main reason Transition difficulty is Easy rather than Very Easy.

## Needed modifications to the draft under study

| # | Section / location in draft | Current concept or text | Proposed change | Category | Rationale |
|---|---|---|---|---|---|
| 1 | §1, goals bullet | "Simplify Rule management between SCHC endpoints" | "Simplify Rule and Context management between SCHC Endpoints" | REQUIRED FOR TERMINOLOGY MIGRATION | Use -06 capitalized terms; Rule management is Context management in -06 |
| 2 | §2.2, first paragraph and Figure 1 caption | "rule management between two SCHC endpoints" / "Rule Management between two SCHC end-points" | "Rule management between two SCHC Endpoints whose Instances share a Context" / caption "Rule Management between two SCHC Endpoints" | REQUIRED FOR TERMINOLOGY MIGRATION | Resolve "end-point" into -06 Endpoint/Instance model |
| 3 | §2.2, scenario bullet | "SCHC nodes S1 and S2 compress and decompress the traffic using Rules they share." | "SCHC Endpoints S1 and S2 each host a SCHC Instance; the two Instances compress and decompress the traffic within a Session, using a shared Context (Set of Rules)." | REQUIRED FOR TERMINOLOGY MIGRATION | Map "Rules they share" to the -06 Context/Session model; local rewrite, semantics unchanged |
| 4 | §2.2, problem bullet | "how to identify this new option in the Rule and communicate this identifier to S2" | "…so that, when the updated Context is distributed to S2 (e.g., by the Domain Manager or a dynamic Context update mechanism), S2 can understand which option is involved…" | REQUIRED FOR TERMINOLOGY MIGRATION | Frames Rule communication as Context distribution/update per -06, preserving the draft's ambiguity about the managing entity |
| 5 | §2.3, second bullet | "making Rule exchange between them problematic" | "making the exchange of Rules (Context synchronization) between them problematic" | REQUIRED FOR TERMINOLOGY MIGRATION | Aligns "Rule exchange" with the -06 Context-synchronization concept |
| 6 | §3.3, advantages paragraph | "enabling different SCHC implementations to exchange rules for any option" | "enabling different SCHC implementations to exchange Rules (synchronize Contexts) for any option" | REQUIRED FOR TERMINOLOGY MIGRATION | Same as #5 |
| 7 | §5.2 | "both SCHC endpoints must process entries in the same order" | "all Instances participating in a Session must process entries in the same order" | REQUIRED FOR TERMINOLOGY MIGRATION | The obligation binds the Instances of a Session in -06 terms; also generalizes correctly to N>2 |
| 8 | §5.2 | "*This constraint should be documented in [I-D.ietf-lpwan-architecture] to ensure interoperability" | "This constraint is stated in the Context consistency considerations of [I-D.ietf-schc-architecture]: the order of entries is part of the Context and is preserved by Context distribution and synchronization." | REQUIRED FOR TERMINOLOGY MIGRATION | Redirects the obsolete architecture reference to -06 §6.2 (assumes the Trivial edit of `schc-architecture-edits.md` is accepted) |
| 9 | §5.3, third bullet | "It allows for cleaner Rule exchange between SCHC endpoints" | "It allows for cleaner Rule exchange between SCHC Endpoints (Context synchronization within a Domain)" | REQUIRED FOR TERMINOLOGY MIGRATION | -06 terms; names the Domain scope of Context sharing |
| 10 | §5.4.3 | "When transmitting Rules from one endpoint to another, the order of Field Descriptors must be preserved" | "When a Context is distributed or synchronized between Instances, the order of Field Descriptors must be preserved" | REQUIRED FOR TERMINOLOGY MIGRATION | Rule transmission = Context distribution/synchronization in -06 |
| 11 | §5.4.3 | "This requirement should be explicitly documented in [I-D.ietf-lpwan-architecture] to clarify that:" and "The order of entries should not be changed when transmitted between endpoints" | Reference redirected to [I-D.ietf-schc-architecture]; "…when a Context is transmitted between Endpoints" | REQUIRED FOR TERMINOLOGY MIGRATION | Same as #8; normative strength ("should not") preserved |
| 12 | §5.4.3, second bullet | "must be processed in a consistent sequence across all implementations" | "must be processed in a consistent sequence across all Instances" | REQUIRED FOR TERMINOLOGY MIGRATION | Precision: the processing entity is the Instance |
| 13 | §6.2, Informative References | [I-D.ietf-lpwan-architecture] (draft-ietf-lpwan-architecture-02, 2022) | Replace with [I-D.ietf-schc-architecture] (draft-ietf-schc-architecture-06) | REQUIRED FOR TERMINOLOGY MIGRATION | The referenced document is superseded by the document under comparison |
| 14 | §2.2 scenario | Two-endpoint example presented without qualification | Add a sentence noting the topology is illustrative and the mechanism applies equally when a Context is shared by more than two Instances or across multiple Sessions | OPTIONAL CLARIFICATION | Prevents readers inferring a 2-party constraint; not required for migration |
| 15 | §5.1 | "values provided by the SCHC Working Group" (space-id) | Clarify the allocation mechanism (e.g., IANA registry established by the 9363 revision) | OPTIONAL CLARIFICATION | Identifier-authority precision; not architecture-related |
| 16 | Running header | "Internet-Draft SCHC for CoAP" | "Internet-Draft SCHC Universal Options" (or similar) | EDITORIAL | Header inherited from another document |
| 17 | Appendix A, module description | Description text speaks of "compound-ack behavior for Ack On Error" / "RFC YYYY: OAM" | Replace with a description of the option-space augmentation | EDITORIAL | Copy-paste from an unrelated module; misleading but not architectural |
| 18 | Appendix A, identity space-id-coap | description "Field ID base type for IPv6 headers described in RFC 8200" | Describe as the CoAP option space (RFC 7252) | EDITORIAL | Wrong description/reference on a key identity |
| 19 | §5.1 / Appendix D title / §D.4 | "diffently", "Syntatic", "minize", "fith" | Spelling fixes | EDITORIAL | Typos |
| 20 | Appendix A, organization/contact | lpwan WG name and list | Update to the SCHC WG | EDITORIAL | Stale metadata |

## Needed modifications to SCHC Architecture -06

| # | Architecture section | Architecture concept | Current text or definition | Proposed change | Category (ARCHITECTURE GAP / OPTIONAL CLARIFICATION) | Rationale |
|---|---|---|---|---|---|---|
| 1 | §6.2 Context consistency | Context / Set of Rules — order of Field Descriptors within a Rule | §6.2 recommends deploying "the same Context (with identical SoR)" and discusses compatible partial Contexts; nothing states that the order of entries within a Rule is part of the Context or must survive Context distribution | Add one paragraph stating that the order of the Field Descriptors (entries) within a Rule is part of the Context; that residue serialization follows that order; that Context distribution and synchronization must preserve it; and that when a Rule representation contains several lists of entries (e.g., a Data Model and its augmentations), the Context must define a single deterministic processing order (exact text in `schc-architecture-edits.md`) | ARCHITECTURE GAP | The draft normatively points to the architecture for exactly this statement (§5.2, §5.4.3); the relationship is implied by "identical SoR" but a YANG-based Context store may legally reorder entries, so the implication is insufficiently explicit. Additive clarification only — no existing text changes meaning |
| 2 | §3 Terminology, "Context" | Context metadata | "Metadata may, for example, refer to a data model or a parser compatible with the rule format." | Optionally extend the example list with "or the ordering conventions for the entries of a Rule" | OPTIONAL CLARIFICATION | Convenience cross-link only; item 1 alone closes the gap |

## Final migration assessment
- Can the draft be migrated without changing technical behavior? **Yes**
- Can the migration be performed mechanically? **Mostly** (a handful of sentence-level rewrites; all mapping decisions clear and repeatable)
- Does the draft expose a SCHC Architecture -06 gap? **Yes** (Trivial: entry-order preservation as part of Context consistency)
- Is the gap required for this draft or merely useful generally? **Required for this draft** — the draft explicitly directs the architecture to document the ordering constraint, so migration cannot complete cleanly without a landing place in -06; the clarification is additionally useful for any Data-Model-based Context distribution
- What is the single most important migration issue? Redirecting the draft's two interoperability pointers from the obsolete [I-D.ietf-lpwan-architecture] to -06's Context-consistency model, which first requires -06 to state explicitly that entry order within a Rule is part of the shared Context.
