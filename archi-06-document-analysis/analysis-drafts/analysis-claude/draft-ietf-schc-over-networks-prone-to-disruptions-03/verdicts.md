# Architectural alignment review: draft-ietf-schc-over-networks-prone-to-disruptions-03

Reference architecture: draft-ietf-schc-architecture-06 ("-06" below).
Both documents analyzed from verbatim local text (see source confirmations in the analysis log).

## Verdicts
- Conceptual equivalence: **High**
- Transition difficulty: **Easy**
- SCHC Architecture adaptation need: **Trivial**

## Grade calibration

| Criterion | Verdict | Why not one grade higher? | Why not one grade lower? |
|---|---|---|---|
| Conceptual equivalence | High | Two concepts need explicit interpretation rather than pure reframing: (1) the SCHC Proxy's "split SCHC connection" must be decomposed into two -06 Sessions sharing one Context, and (2) SCHC-as-object-transport needs the admitted unit to be an application-layer object, which is only implicit in -06 (Stratum). Both need a deliberate mapping decision, so this is not purely a terminology/framing exercise (Very High). | No concept requires reframing with non-natural semantics: every important native concept lands as Direct, Composite, or a natural Profile-specific use after two small clarifications. The draft's core model — static shared Context, F/R reliability between a device-side and a network-side SCHC entity, device-identity-based demultiplexing at a shared gateway — is exactly the -06 model (the -06 LPWAN example in Appendix A.2.1 is nearly isomorphic to the draft's Proxy-with-several-devices case). No mapping ended Partial-after-clarification, Missing, or Misleading, so Medium is not supported. |
| Transition difficulty | Easy | A handful of passages need local rewriting rather than word substitution: the Proxy section (Session decomposition), the configuration-ID section (Domain-scoped Context/Rule-group identification), the flow-identifier passages (Discriminator role), and one text/figure inconsistency (Phase-2 ACK destination). These are composite rewrites, not one-word replacements, so Very Easy is not supported. | The edits are localized, the mapping decisions are clear and repeatable across occurrences, no section needs restructuring, and the complete migration diff (File 3) was producible without repeated architectural judgment — each judgment point was resolved once and applied mechanically thereafter. That rules out Medium. |
| SCHC Architecture adaptation need | Trivial | Two notions required by the draft are implicit rather than explicit in -06: (a) that the unit admitted to an Instance may be an application-layer object (Stratum reaching the application layer), and (b) that an Endpoint may sit on an intermediate node and participate in distinct Sessions toward each side (proxy/relay deployment). Both require short additive clarifications, so None is not supported. | Both edits are purely additive (a sentence added to the Stratum definition; a paragraph added to the Session section, stating relationships that -06 already permits — multiple Instances per Endpoint, multiple Sessions per Domain, arbitrary topologies). No existing definition or normative statement changes meaning, and no concept is added, removed, or re-scoped; per the rubric that is Trivial, not Medium. |

## Executive assessment

SCHC Architecture -06 **can naturally express** the draft under study. The draft describes SCHC deployments over disruption-prone networks (cellular Zero-Energy / Ambient IoT devices and Direct-to-Satellite IoT), and its technical model — a constrained device and a network-side SCHC termination sharing a static context, with delay-adapted F/R parameters, an optional on-path SCHC proxy, object (rather than IP-packet) transport, payload compression, and pre-provisioned parameter sets selected by identifiers — decomposes cleanly onto -06's Endpoint / Instance / Context / Session / Domain / Discriminator / Instance Configuration / SoV model.

The **principal conceptual mapping**: the draft's device-side and network-side SCHC entities (Dev, Proxy, SCHC Gateway, Application Server) each become an Endpoint hosting one or more Instances; the draft's "SCHC connection / session / flow" vocabulary resolves onto the -06 Session (with the draft's per-object "session" being an F/R exchange *within* a Session); the SCHC Proxy becomes an intermediate Endpoint whose Instances participate in two Sessions sharing the same Context — a decomposition the draft itself already articulates ("Local acknowledgments split the SCHC connection"); the onboarding/device identifier becomes a Discriminator used by the Dispatcher; "configuration ID" / "context groups" become Domain-scoped Contexts or groups of F/R Rules; BETI/TC/MAX_OBJECT_SIZE become Instance Configuration parameters; configured timers become F/R Rule parameters with runtime state in the SoV.

The **principal migration difficulty** is resolving the draft's overloaded connectivity vocabulary ("SCHC session", "SCHC connection", "SCHC flow", "endpoint" used both for a platform API endpoint and a SCHC termination) into distinct -06 concepts, and rewriting the Proxy passages as a two-Session structure.

An **Architecture gap exists but is Trivial**: -06 should (a) make explicit that the Stratum may extend to the application layer, so that an object is a legitimate unit admitted to an Instance, and (b) state explicitly that an Endpoint may be deployed on an intermediate node participating in Sessions toward each side (proxy/relay). Both are additive clarifications of relationships -06 already implies; they are proposed in `schc-architecture-edits.md`.

## Native conceptual model

| Native draft concept | Meaning in the draft | Owner / logical location | Scope | Identifier or selector | Cardinality and relationships | Notes |
|---|---|---|---|---|---|---|
| Network prone to disruptions (NPD) | Network with long delays / intermittent connectivity; buffering + data forwarding | The deployment environment | Network | — | One NPD contains many Devs and network elements | Motivating environment, not a protocol entity |
| Dev / ZE device / end device | Constrained (often energy-harvesting) device producing mostly-uplink traffic; runs SCHC ("Dev (SCHC)") | Physical device at network edge | Node | Onboarding/device identifier | One Dev ↔ one network-side SCHC termination (via proxy or direct); many Devs per network/Proxy | Device types A/B/C (3GPP TR 38.848) differ in energy/RX capability |
| BS / IN / AN / UE / Ground Station | Cellular / satellite connectivity elements (base station, intermediate node, assisting node, user equipment, ground station) | Radio access / ground segment | Link / network | — | Topologies 1–4 (cellular); Dev–LEO–GndSta chain (DtS) | Provide connectivity; no SCHC processing attributed to them |
| Operator platform / data collection platform | Mediates device data toward applications; hosts SCHC "between network and terminal" or over-the-top; exposes APIs / pub-sub; onboards devices | Operator network or third party | Network / administrative | Device identifier exposed via API | One platform : many Devs : many applications | Application-facing exposure is outside SCHC processing proper |
| Proxy (SCHC) (Section 4.1) | Middle host maintaining connection state across disruptions; may answer requests instead of the Dev | Intermediate host between Dev and App Server | Peer-pair (both legs) | — | One Proxy : many Devs ("If the Proxy has several devices attached, it must recognize which one is sending") | SCHC-processing entity on the path |
| LEO satellite | Store-and-forward node; in §4.4.1 acts as SCHC Proxy issuing local ACKs / local retransmissions | Satellite | Link (per pass) / peer-pair when Proxy | — | One satellite serves many end devices and ground stations | Two roles: transparent store-and-forward vs. SCHC Proxy |
| SCHC Gateway (SCHC GW) | Network-side SCHC termination in DtS-IoT (reassembler; sends end ACKs) | Terrestrial network side | Peer-pair with each Dev | — | One GW : many end devices | Called "endpoint where SCHC is terminated in the network" in §3.1.2 |
| Application Server (App, SCHC) | The far-side SCHC entity in the general architecture (Figure 10) | Application side | Peer-pair with Proxy | — | One App Server : one Proxy : many Devs (figure) | |
| SCHC context / "contexts and rules" | Static shared compression/fragmentation information between sender and receiver | Both SCHC peers | Peer-pair (shared) | Rule (RuleID implicit via RFC 8724) | One context shared by ≥2 peers; a device may have several context groups | Provisioning options for no-RX devices: firmware, flashing, NFC, well-known rules |
| Well-known rule | A Rule that never needs updating, pre-agreed for a device class | Both peers (pre-provisioned) | Device-class / network | Rule identity | Shared across many devices | Enables zero-provisioning operation |
| Configuration ID / pre-configured configurations / "context groups" / "rule groups" | Network-produced parameter sets (packet size, timers) addressed individually | Network (producer); both peers (consumers) | Network (unstated precisely) | Configuration ID | One device : several configurations (large/medium/small; delay classes) | Scope/assigner of the ID left implicit — ambiguity |
| Onboarding / flow identifier | Unique identifier assigned at onboarding, "associated with the SCHC flow"; maps transmissions to applications; lets the Proxy recognize which device sends | Platform / network assigns; device may get it out-of-band | Platform / network | The identifier itself | 1 identifier : 1 device : 1 SCHC flow | Also carried to applications via API |
| "SCHC session" / "SCHC connection" / "SCHC flow" | Loosely: the SCHC communication relationship, or a single fragmented-object transfer ("SCHC object transfer session", Fig. 8/12 captions) | Between the two SCHC peers | Peer-pair; per transfer for the "session" usage | W / FCN within a transfer | One Dev : one relationship : many object transfers | Overloaded vocabulary — three terms, two meanings |
| MAX_OBJECT_SIZE | Threshold triggering delay-friendly transmission configuration | Device (configured at onboarding or out-of-band) | Node-local | — | 1 per device | |
| BETI + Tile Count (TC) | Pacing: after each Best Effort Transfer Interval the device sends TC tiles | Device (configured by network) | Node-local | — | 1 BETI config per device; TC default 1 | Timer-, occasion-, or radio-event-based |
| F/R parameters (retransmission/inactivity timers, Attempts, MAX_ACK_REQUESTS, window size, tile size, MAX_PACKET_SIZE) | Configured values matched to delay class; running timers govern a transfer | Configured by network; running state at fragmenter/reassembler | Configured: network-wide per class; running: per transfer | Via configuration ID / rule group | Several classes (extra-long … no delay); values correlated to window size | Static values vs. running state distinguished implicitly |
| Object transport | SCHC F/R used as "a simple transport protocol for the whole object instead of only fragmenting IP packets", "different from what has been specified by RFC [RFC8724]"; object ≈ imaginary jumbo IP packet cut into tiles | Both SCHC peers | Peer-pair | W/FCN per transfer | One object : many tiles : windows | Central mechanism of Section 4 |
| Payload compression | SCHC C/D applied to payload fields (SenML/JSON example); "a section of the context MUST be dedicated to the payload" | Both SCHC peers | Peer-pair (shared context) | FID naming scheme (e.g., application/senml+json.bn.1) | One rule : many payload fields | Requires format-aware parsing logic |
| Time-based / size-based profiles | 4 latency categories (hours/day/week/month) + packet-size categories fixing timer values | Deployment / network | Device-class | Profile identity | One profile : many devices | |
| FEC mechanism | End-device (fragmenter) and SCHC gateway (reassembler) protect tiles with FEC; ACK becomes optional (MAY) | Both SCHC peers | Peer-pair | — | Alternative to Proxy mechanism | Details in draft-munoz-schc-over-dts-iot |
| Local acknowledgment / responsibility transfer | Proxy ACKs regular fragments locally; "Local acknowledgments split the SCHC connection"; responsibility for acknowledged fragments moves to the Proxy | Proxy | Per leg (Dev–Proxy) | ACK/bitmap (W, FCN, C) | One Proxy leg + one Proxy–GW leg per transfer | Downlink: local retransmissions |
| Store-and-forward; visibility/revisit windows | Queuing until connectivity; DtS timing structure (visibility vs. revisit time) | End devices, satellites | Link / environmental | — | — | Drives the very long timer values |

## Native architectural model

The draft is an informational description of how SCHC improves communication in networks prone to disruptions (NPDs). It develops two scenario families — cellular Zero-Energy ("Ambient IoT") devices, and Direct-to-Satellite IoT — and, from them, a common technical proposition: use SCHC compression and, above all, SCHC ACK-on-Error fragmentation with delay-adapted parameters as a size- and delay-optimized transmission mechanism.

In its own vocabulary, the communicating parties are a **Dev** (ZE device, LPWA device, or DtS end device) on one side and a network-side SCHC termination on the other — variously called "the endpoint where SCHC is terminated in the network", the **SCHC Gateway**, or the **Application Server (SCHC)**. Between them, the general architecture of Section 4.1 inserts a **Proxy (SCHC)**: a middle host that maintains connection state while communication with the Dev is disrupted and keeps communicating with the Application Server, possibly answering requests on the Dev's behalf. In the DtS-IoT scenario the LEO satellite can play this Proxy role, issuing local acknowledgments uplink and local retransmissions downlink; the draft states explicitly that local acknowledgments "split the SCHC connection between the end-device and SCHC Gateway", with responsibility for acknowledged fragments transferring to the proxy. When the satellite is not a proxy it is a pure store-and-forward element that buffers SCHC fragments between visibility windows without SCHC semantics.

The shared state is the classic SCHC static **context** ("contexts and rules"), provisioned before use. Because the most constrained devices cannot receive at all, the draft enumerates provisioning options that are strictly one-way or out-of-band: standardized rule sets in firmware, flashing, NFC, or **well-known rules** provisioned at device configuration. Context updates are expected to be rare; instead, the network pre-produces several parameter sets — "context groups" or "rule groups" for large/medium/small packet sizes and for delay classes ranging from "no delay" to "extra-long delay" — each addressed by a **configuration ID**. The scope and assigner of this identifier are left implicit.

Device-local operating parameters form a separate layer of state: **MAX_OBJECT_SIZE** (the threshold above which delay-friendly operation is selected), **BETI** (a pacing interval based on timers, transmission occasions, or radio events), and **TC** (tiles sent per interval). These are configured at onboarding or out-of-band and are not shared compression state. Configured F/R values (retransmission and inactivity timers up to months, Attempts, MAX_ACK_REQUESTS, window size, tile size) belong to the network-produced parameter sets; the running timers and bitmaps live at the fragmenter and reassembler for the duration of a transfer.

Identification is handled by an onboarding identifier: the device "has to be onboarded in the platform with a unique identifier that is associated with the SCHC flow", used to map transmissions to consuming applications; and, on the data path, "If the Proxy has several devices attached, it must recognize which one is sending". So a single per-device identifier simultaneously demultiplexes the data path and anchors the application-facing exposure.

The processing model deliberately extends the SCHC input beyond IP: a complete application object is treated as if it were "an imaginary jumbo IP package" that SCHC fragments into tiles fitted to the radio transport block, so that "SCHC would become a simple transport protocol for the whole object … which is different from what has been specified by RFC [RFC8724]". Complementarily, the draft proposes payload compression — reusing the C/D engine on payload fields (a SenML/JSON example is worked through), with "a section of the context MUST be dedicated to the payload" — and, in the DtS scenario, an FEC alternative where encoded tiles make the SCHC ACK optional.

Communication relationships are point-to-point per device at SCHC level (Dev ↔ network termination), but the network-side entities are multipoint: one Proxy or Gateway serves many devices, and network-initiated transmission may target "a large number of devices". Ancillary cellular elements (BS, IN, AN, UE) and the ground station provide connectivity only; no SCHC processing is attributed to them.

Management is assumed but thin: onboarding, out-of-band configuration, network-produced configuration sets, and buffering policy ("configuration … in some cases at the devices and in others at the network, or both"). Security "follows [RFC8724]"; trusted provisioning of contexts and identifiers is implicit. Nothing in the draft's model depends on negotiation — everything shared is static and pre-provisioned, exactly the RFC 8724 assumption.

## Concept mapping

(Final table, after Pass 3 corrections. "Edit 1"/"Edit 2" refer to the Trivial architecture clarifications in `schc-architecture-edits.md`.)

| Native draft concept | Meaning in the draft | SCHC -06 mapping | Match type | Scope alignment | Cardinality alignment | Semantic differences | Notes |
|---|---|---|---|---|---|---|---|
| NPD / disruptions / visibility & revisit windows | Environment with long delays, intermittent links | Environment characteristics captured by a deployment profile's F/R parameter choices (Section 5 of -06) | Profile-specific | Aligned (environmental, out of architecture) | — | None | -06 explicitly relaxes the "intermittent connectivity" inheritance; profiles may constrain for it |
| Dev / ZE / end device (as SCHC party) | Physical device running SCHC | Physical device **hosting** an Endpoint that hosts one Instance | Composite | Aligned (node hosts logical Endpoint) | Aligned: -06 allows 1 Endpoint / 1 Instance as a permitted restriction | -06 separates logical Endpoint from physical device; draft conflates them benignly | Same simplification as -06's own LPWAN example |
| Device types A/B/C | Energy/RX capability classes | Profile constraints (provisioning restrictions, no-downlink operation) | Profile-specific | Aligned | — | None | Type A/B (no reception) ⇒ pre-provisioned static Context only — exactly the RFC 8724 / -06 base assumption |
| BS / IN / AN / UE / Ground Station | Connectivity elements without SCHC processing | Not applicable — underlying network below the SCHC Stratum | Not applicable | Not applicable | Not applicable | None | No SCHC semantics to map |
| Operator / data collection platform | Hosts network-side SCHC; onboards devices; exposes data | Endpoint (+ Instances) hosted on the platform, plus Domain-management functions (onboarding ≈ registration with Domain Manager); API/pub-sub exposure is application-level, outside SCHC | Composite | Aligned | Aligned (1 platform : N Devs = 1 Endpoint : N Sessions) | Application exposure out of -06 scope (also out of SCHC scope in the draft) | |
| Proxy (SCHC) / LEO satellite as SCHC Proxy | On-path SCHC entity; local ACKs/retransmissions; splits the "SCHC connection" | Intermediate **Endpoint** hosting Instance(s) participating in **two Sessions** that share the same Context (same Domain): Dev↔Proxy and Proxy↔GW/App Server | Composite | Aligned after Edit 2 (intermediary deployment made explicit) | Aligned: 1 Proxy Endpoint : N Devs : N Session pairs | Local-ACK/responsibility-transfer *behavior* is protocol-level, defined by the profile (e.g., draft-munoz), not by the architecture | The draft's own words ("split the SCHC connection") already describe the two-Session decomposition |
| LEO satellite (store-and-forward only) | Buffers fragments between passes, no SCHC semantics | Not applicable — underlying network element | Not applicable | Not applicable | Not applicable | None | Fig. 8 end-to-end case |
| SCHC Gateway / Application Server (SCHC) | Network-side SCHC termination | Endpoint hosting the network-side Instance(s) | Composite | Aligned | Aligned: -06 LPWAN example covers both "one shared Instance, one Session per Device" and "one Instance per Session" | None | Near-isomorphic to -06 Appendix A.2.1 |
| SCHC context / "contexts and rules" | Static shared C/D + F/R information | Context (containing the Set of Rules) | Direct | Aligned: shared by ≥2 Instances; Instances sharing Contexts form a Domain | Aligned | None | |
| Well-known rule | Never-updated pre-agreed Rule | Rule (with RuleID) in a pre-provisioned Context shared Domain-wide | Direct | Aligned (RuleID meaningful within its Context; Context shared across the Domain) | Aligned (1 Context : N devices) | None | |
| Context provisioning options (firmware, flashing, NFC, well-known) | One-way / out-of-band provisioning for no-RX devices | Context provisioning via Domain Manager / Context Repository, degenerating to manufacture-time pre-provisioning | Composite | Aligned: -06 assumes Context "static and provisioned before use" | Aligned | Domain Manager cannot *synchronize* toward a device that cannot receive — but static pre-provisioning is the -06 base case | No dynamic update claimed for those devices |
| Configuration ID / "context groups" / "rule groups" | Pre-produced parameter sets addressed individually | Multiple **Contexts** (identified within the Domain), or groups of **F/R Rules** (RuleIDs) with distinct parameters within one Context | Composite | Requires the draft to state the scope explicitly: -06 makes Context IDs unique within the Domain | Aligned (1 device : N Contexts/Rule groups permitted) | Draft leaves assigner/uniqueness implicit — ambiguity, resolved by adopting Domain scope | Both readings (per-Context vs. per-Rule-group) preserve behavior; migration keeps both available |
| Onboarding / flow identifier | Demultiplexes devices at the Proxy/platform; anchors application mapping | **Discriminator** used by the **Dispatcher** to route Datagrams to the appropriate Instance/Session; assignment via Domain management (registration) | Direct | Aligned: -06 leaves Discriminator scope to the deployment; draft's platform/network scope fits | Aligned (1 identifier : 1 device : 1 Session, N Sessions at gateway) | None — -06's LPWAN example uses device identity as Discriminator, exactly this pattern | |
| "SCHC connection" / "SCHC flow" | The SCHC communication relationship | Session | Direct | Aligned (peer-pair ⊂ "two or more Instances") | Aligned | None | Pure rename |
| "SCHC session" (as used for one object transfer) | One fragmented-object F/R exchange | An **F/R exchange within a Session** (running state = SoV) | Partial | Aligned once reworded | Aligned (1 Session : N transfers) | Term overload: -06 Session is the standing relationship, not one transfer | Cannot be a one-word rename; rewording required (done in File 3) |
| MAX_OBJECT_SIZE, BETI, TC | Device-local operating/pacing parameters | Instance Configuration parameters | Direct | Aligned (node-local → Instance-local) | Aligned (1 per Instance) | None | "Local parameters of the Instance" is -06's exact definition |
| Configured F/R values (timers, Attempts, MAX_ACK_REQUESTS, window/tile size) | Delay-class parameter sets + running transfer state | Configured values = F/R Rule parameters within the Context; running timers/counters/bitmaps = **Set of Variables (SoV)** per Session | Composite | Aligned | Aligned (SoV is per-Session in -06; draft's per-transfer state lives in the Session's SoV) | None | Clean static-vs-runtime split that the draft makes only implicitly |
| Object transport (SCHC as transport protocol) | F/R applied to a whole application object | Instance whose **Stratum** is the application layer; the admitted unit is the object; F/R provides segmented delay-tolerant transport | Profile-specific (after Edit 1; Partial against unmodified -06) | Aligned after Edit 1 | Aligned | The *protocol* extension (object input instead of SCHC Packet of a compressed IP packet) is beyond RFC 8724 and remains profile/extension work in both documents | The draft itself flags the RFC 8724 divergence; the architecture only needs to name the placement |
| Payload compression | C/D on payload fields (SenML) | Rules whose FIDs address payload fields + a Parser/Data Model for the payload format, specified by the Context | Profile-specific | Aligned (-06 Parser definition explicitly includes "payload data"; Context "may specify which Parser to use") | Aligned | None | Natural use of -06 extensibility, not a stretch |
| Time-/size-based profiles | Latency & size categories fixing parameters | Deployment Profile constraints (Section 5 of -06) selecting F/R Rule parameter sets | Direct | Aligned | Aligned | None | |
| FEC mechanism | Tile protection making ACK optional | An additional SCHC function of the Instances (naturally a **SCHClet**), declared in the Instance Configuration Manifest; protocol defined by the companion draft | Profile-specific | Aligned | Aligned | The FEC *protocol* is out of -06 scope (as it is out of this draft's scope — delegated to draft-munoz) | SCHClet = "self-contained unit implementing a specific SCHC function" — natural fit |
| Local acknowledgment / responsibility transfer | Proxy ACKs locally; owns delivery afterwards | Ordinary SCHC ACK **within the Dev↔Proxy Session**; the responsibility rule is profile-level protocol behavior spanning the two Sessions | Composite | Aligned after Edit 2 | Aligned | -06 does not (and need not) define reliability delegation; the architecture expresses the structure, the profile the behavior | |
| Store-and-forward buffering / network caching (§4.3) | Queue until connectivity; cache downlink objects | Long F/R timer parameters + SoV persistence at the Instances; caching below or beside SCHC is out of scope | Profile-specific | Aligned | Aligned | None | |
| Network-initiated transmission (§4.2.2) | Network needs a copy of the object; APIs toward AS | Compressing Instance hosted on a network-side Endpoint; object staging/APIs are application/management plane, outside SCHC processing | Composite | Aligned | Aligned (N devices ⇒ N Sessions; -06 Session also admits >2 Instances if a profile wants true multipoint) | Draft is ambiguous between N unicast transfers and true multicast; both are expressible (Session is "two **or more** Instances") | Ambiguity noted; no forced interpretation needed |

## Scope and cardinality comparison

| Relationship or identifier | Draft model | Architecture -06 model | Alignment | Consequence |
|---|---|---|---|---|
| Ownership of Context | Shared between sender and receiver ("common context is shared between sender and receiver"); network produces it | Context shared by two or more Instances; managed/distributed by Domain Manager via Context Repository | Aligned | Rename + optional attribution of the producing role to the Domain Manager |
| Ownership of Set of Rules | "Contexts and rules" set up by the network; well-known rules pre-provisioned | SoR is contained in the Context | Aligned | Pure terminology |
| Ownership of Set of Variables | Running timers/bitmaps at fragmenter & reassembler per transfer | SoV is per-Session runtime state | Aligned | Migration names the implicit runtime state "SoV" |
| Endpoint ↔ SCHC Instance | Implicit 1:1 (each device "uses SCHC") | 1:N permitted; 1:1 is a valid restriction | Aligned (draft is a permitted restriction) | None |
| SCHC Instance ↔ Session | Proxy/GW "has several devices attached" — one network entity, many device relationships | Either one shared Instance with one Session per Device, or one Instance per Session — both explicitly documented in -06 Appendix A.2.1 (Tables 1 and 2) | Aligned | The draft need not choose; migration can leave both implementations open, as -06 does |
| Sharing of Context between Sessions/Instances | Well-known rules and network-produced sets shared across many devices | Same Context/SoR usable by all Sessions; SoV distinct per Session | Aligned | None |
| RuleID scope | Implicit per shared context (RFC 8724) | RuleID identifies a Rule within a Context | Aligned | None |
| Configuration ID scope | Unstated (network-produced, addressed individually) | Context and Instance identifiers unique within the Domain | Alignable — draft must state the scope | REQUIRED FOR CONCEPTUAL ALIGNMENT edit: scope the identifier to the Domain (as Context ID or F/R Rule-group selector) |
| Onboarding / device identifier (Discriminator) scope | Unique within the platform/network; assigned at onboarding, possibly out-of-band | Discriminator scope deployment-defined; -06 example uses LPWAN device identity | Aligned | None |
| Control Header processing scope | Not applicable — the draft defines no Control Header (its identifiers travel out-of-band or in lower layers) | Optional Control Header with profile-defined presence/format | Not applicable | None; a future revision could optionally carry the device/Session identifier in a Control Header, but nothing requires it |
| Domain membership and boundaries | Implicit: one operator network / one DtS deployment; Dev, Proxy, GW share the same contexts | Domain = Instances sharing a common set of Contexts, run by a Domain Manager | Aligned (implicit single Domain) | OPTIONAL clarification: state that Dev, Proxy, and GW Instances belong to one Domain |
| Dispatcher placement | Implicit at the Proxy/platform ("must recognize which one is sending") | Dispatcher is an Endpoint component using the Discriminator | Aligned | Terminology only |

## Challenged mappings

| Concept | Initial mapping | Challenge | Final mapping | Reason for change |
|---|---|---|---|---|
| Proxy (SCHC) / LEO as SCHC Proxy | Direct → "Endpoint" | A single -06 concept does not capture the relay role: does the Dev's Session terminate at the Proxy or at the GW? Does the mapping survive multiple devices and the GW ACK in Fig. 11? Checked against the draft's own text: "Local acknowledgments **split the SCHC connection** between the end-device and SCHC Gateway" — the draft itself asserts two segments. Two Sessions sharing one Context (one Domain) preserve semantics, scope, and cardinality, and survive the multi-device case (one Session pair per device). The unmodified -06 never shows an intermediate Endpoint, so the naturalness relies on an implicit permission | Composite (Endpoint + Instances + two Sessions sharing one Context), with Edit 2 making the intermediary deployment explicit | Single-concept mapping hid the Session decomposition; also surfaced the Trivial architecture gap (Edit 2) |
| "SCHC session" (Fig. 8/12 captions, "SCHC object transfer session") | Direct → "Session" | A blind rename would equate one object transfer with the -06 Session, silently changing the -06 term's meaning (a Session persists across transfers; the -06 LPWAN example has *implicit, standing* Sessions). One transfer is an F/R exchange whose state is the SoV | Partial (an F/R exchange within a Session) | Term overload detected; rename alone would be Misleading, rewording required |
| Object transport | Profile-specific | Natural-use test against unmodified -06: -06's vocabulary is packet-oriented (Dispatcher "routes packets", Parser "dissects network packets") and -06 states SCHC mechanisms "are used as defined" in RFC 8724, while the draft admits divergence from RFC 8724. Is admitting an application object a natural use, or creative reinterpretation? The Stratum concept ("contiguous layers within which SCHC processing can be applied") accommodates the application layer, but only implicitly | Partial against unmodified -06; naturally Profile-specific once Edit 1 (Stratum may reach the application layer; admitted unit may be an object) is accepted | Natural-use rule: an implicit accommodation is not a natural use until stated; drives the Trivial adaptation verdict |
| Configuration ID | Direct → "Context identifier" | The draft's text supports two readings: distinct Contexts ("three context groups") or parameter-distinct F/R Rule groups within one Context ("one of the rule groups"). The draft also never states who assigns the ID or where it is unique. Picking "Context ID" silently resolved an ambiguity | Composite (Domain-scoped Context identifiers **or** groups of F/R Rules within a Context; ambiguity preserved and flagged) | Uncertainty must be preserved, not optimized away; draft clarification required |

## Architectural risk points

- **Risk:** Session-semantics overload around the Proxy. The migrated text must not imply that -06 *defines* proxy behavior (local ACK, responsibility transfer); -06 only expresses the structure (intermediate Endpoint, two Sessions, shared Context). The behavior belongs to the profile/protocol (draft-munoz-schc-over-dts-iot).
  **Why it matters:** Readers could take reliability delegation as an architectural property of Sessions, corrupting the -06 Session concept.
  **Consequence for migration:** The File 3 diff phrases the split structurally and leaves the responsibility rule as profile behavior; Edit 2 says explicitly "This document does not define such proxy procedures."

- **Risk:** The draft's "session/connection/flow" triple maps onto one -06 term (Session) plus one non-term (F/R exchange). A mechanical rename of every "session" would produce statements that are false in -06 vocabulary (e.g., a "Session" that starts and ends with one object).
  **Why it matters:** This is the highest-volume source of migration error.
  **Consequence for migration:** Each occurrence classified individually in File 3; this is what keeps Transition at Easy rather than Very Easy.

- **Risk:** Configuration ID scope is unstated (who assigns it, where it is unique, where it is carried — data path or provisioning plane only?).
  **Why it matters:** In -06, identifiers for Instances, Contexts, and Sessions are unique within the Domain; an unscoped identifier cannot be placed in the model.
  **Consequence for migration:** REQUIRED FOR CONCEPTUAL ALIGNMENT edit scoping it to the Domain; the per-Context vs. per-Rule-group ambiguity is flagged for the authors rather than silently resolved.

- **Risk:** Object transport remains a protocol extension beyond RFC 8724 even after migration. The architecture names its placement (application-layer Stratum) but does not make it interoperable.
  **Why it matters:** Migration to -06 vocabulary must not be mistaken for RFC 8724 conformance of the object-transport mode.
  **Consequence for migration:** The migrated text keeps the draft's own caveat ("different from what has been specified by [RFC8724]") verbatim.

- **Risk:** §4.4.1.1 text/figure inconsistency: the text says the SCHC gateway "responds **to the end device** with an SCHC ACK", while Figure 11 shows that ACK terminating at the LEO satellite (the end device already got the local ACK).
  **Why it matters:** Under the two-Session mapping the GW's ACK peer is the Proxy; the text as written contradicts both the figure and the proxy mechanism it describes.
  **Consequence for migration:** One REQUIRED FOR CONCEPTUAL ALIGNMENT edit resolving the text in favor of the figure. This is the only place where migration touches a technical statement, and it aligns the text with the draft's own figure rather than changing intended behavior.

## Needed modifications to the draft under study

| # | Section / location in draft | Current concept or text | Proposed change | Category | Rationale |
|---|---|---|---|---|---|
| 1 | §4.4.1 | "Local acknowledgments split the SCHC connection between the end-device and SCHC Gateway." | Recast structurally: the Proxy is an Endpoint hosting Instance(s); local acknowledgments split the end-to-end exchange into two Sessions sharing the same Context (same Domain): Dev↔Proxy and Proxy↔Gateway | REQUIRED FOR CONCEPTUAL ALIGNMENT | -06 has no "SCHC connection"; the split the draft already describes must be expressed as Session decomposition |
| 2 | §4.4.1.1, Phase 2 | "The SCHC gateway receives the tiles and responds to the end device with an SCHC ACK message." | "…responds to the SCHC Proxy with a SCHC ACK message (see Figure 11)." | REQUIRED FOR CONCEPTUAL ALIGNMENT | Under the two-Session structure the GW's Session peer is the Proxy; Figure 11 already shows the ACK terminating at the LEO satellite — resolves a text/figure inconsistency in favor of the figure |
| 3 | §4.2.3.2 | "a set of pre-configured configurations that are addressed individually with a configuration ID … use three context groups" | Identify the parameter sets as Contexts (or groups of F/R Rules within a Context) whose identifiers are unique within the Domain; state who assigns them (Domain management) | REQUIRED FOR CONCEPTUAL ALIGNMENT | The identifier's scope and assigner are unstated; -06 requires Domain-scoped identification of Contexts. The per-Context vs. per-Rule-group ambiguity should be resolved by the authors |
| 4 | §3.1.2.1 and §4.2.1 | Onboarding identifier "associated with the SCHC flow"; "If the Proxy has several devices attached, it must recognize which one is sending" | State that this identifier serves as the Discriminator used by the Dispatcher on the network-side/Proxy Endpoint to route SCHC Datagrams to the appropriate Instance/Session | REQUIRED FOR CONCEPTUAL ALIGNMENT | Gives the identifier its -06 role and scope; matches -06's own LPWAN example (device identity as Discriminator) |
| 5 | §4 (first paragraph) | "SCHC would become a simple transport protocol for the whole object instead of only fragmenting IP packets" | Add the architectural framing: the Instances' Stratum is the application layer and the unit admitted to the Instance is the object itself (keeping the draft's RFC 8724 caveat verbatim) | REQUIRED FOR CONCEPTUAL ALIGNMENT | Anchors object transport to the (clarified) Stratum concept instead of the "imaginary jumbo IP package" device (which may be kept as an intuition) |
| 6 | §3.1.2 (last paragraph) | "how to configure the SCHC fragmentation and reassembly entities. A Dev using SCHC and the endpoint where SCHC is terminated in the network with the relevant context information…" | "how to configure the SCHC Instances that perform fragmentation and reassembly. The Dev and the network each host a SCHC Endpoint whose Instances share the relevant Context…" | REQUIRED FOR TERMINOLOGY MIGRATION | "Entities"/"endpoint where SCHC is terminated" → Instance/Endpoint/Context |
| 7 | §3.1.2 | "the set up of the contexts and rules" | "the provisioning of the Contexts (and their Sets of Rules)" | REQUIRED FOR TERMINOLOGY MIGRATION | Context/SoR are the -06 terms |
| 8 | Global (§3.2.2 Fig. 8 caption, §4, §4.4.2 Fig. 12 caption) | "SCHC session" meaning one object transfer; "SCHC connection"; "SCHC flow" | "Session" where the standing relationship is meant; "F/R exchange within a Session" (or "fragmented SCHC Packet exchange") where a single transfer is meant | REQUIRED FOR TERMINOLOGY MIGRATION | Resolves the overloaded vocabulary; prevents redefining the -06 Session |
| 9 | §4.1 | "Dev … needs a middle host called proxy that will maintain the connection state" | Dev, Proxy, and Application Server each host a SCHC Endpoint; the Proxy maintains the Session state (SoV) across disruptions and participates in Sessions toward both sides sharing the same Context | REQUIRED FOR TERMINOLOGY MIGRATION | Endpoint/Instance/Session/SoV framing of the general architecture |
| 10 | §4.2.1 | "provide pacing information to the SCHC device"; MAX_OBJECT_SIZE/BETI/TC configuration | Name MAX_OBJECT_SIZE, BETI, and TC as parameters of the Dev Instance's Instance Configuration | REQUIRED FOR TERMINOLOGY MIGRATION | These are node-local operating parameters = Instance Configuration in -06 |
| 11 | §4.2.2 | "the compressor entity is in the cellular network" | "the compressing Instance is hosted on an Endpoint in the cellular network" | REQUIRED FOR TERMINOLOGY MIGRATION | Instance/Endpoint vocabulary |
| 12 | §4.2.3.1, §4.2.3.3 | "context" (lowercase, various); "common context is shared between sender and receiver" | "Context … shared between the sending and receiving Instances"; consistent capitalization of Context/Rules | REQUIRED FOR TERMINOLOGY MIGRATION | -06 defined terms |
| 13 | §4.2.3.2 | Timer values "can be also set according to the scheduling calculation…" | Add: configured values are F/R Rule parameters of the Context; running timers/counters are per-Session state in the Set of Variables (SoV) | REQUIRED FOR TERMINOLOGY MIGRATION | Makes the draft's implicit static-vs-runtime split explicit using SoR/SoV |
| 14 | §4.4.2 | "the end-device (fragmenter) and the SCHC gateway (reassembler) use a Forward Error Correction mechanism (FEC)" | Add that the FEC function is a SCHC function of the corresponding Instances (e.g., realized as a SCHClet) indicated in their Instance Configuration | REQUIRED FOR TERMINOLOGY MIGRATION | Places FEC in the -06 function/SCHClet model without defining the protocol |
| 15 | §2 | Conventions and Definitions | Add a paragraph adopting the terminology of draft-ietf-schc-architecture (Endpoint, Instance, Context, Session, Domain, Discriminator, Dispatcher, Instance Configuration, SoR, SoV) | REQUIRED FOR TERMINOLOGY MIGRATION | Anchors all subsequent term usage |
| 16 | §4.2.1 / §4.1 | Proxy with several devices attached | Optionally note that this corresponds to the -06 LPWAN deployment example: one shared Instance with one Session per Device, or one Instance per Session | OPTIONAL CLARIFICATION | Helps implementers; not required for migration |
| 17 | §4.1 / §4.4 | Domain membership implicit | Optionally state that the Dev, Proxy, and Gateway Instances share Contexts and hence belong to a single Domain | OPTIONAL CLARIFICATION | Makes the implicit single-Domain assumption visible |
| 18 | §1 | "…has more information in 3GPPdocA and 3GPPdocB. (REPLACE)" | Replace placeholder with real references | EDITORIAL | Placeholder text |
| 19 | §3 (first paragraph) | "## ZE-Devices based in cellular Zero Energy (ZE) devices…" | Remove the stray "## ZE-Devices based in cellular" markdown artifact | EDITORIAL | Formatting artifact |
| 20 | §3.1.2.1 | "the device might not have the capability to onboard itself to and endpoint" | "…to onboard itself to the platform" | EDITORIAL | Typo; also removes a collision with the -06 term Endpoint |
| 21 | §4.4.1 | "retransmits locally the regular SCHC fragments that losses between SCHC Proxy and end-device" | "…the SCHC regular fragments lost between the SCHC Proxy and the end-device" | EDITORIAL | Grammar |
| 22 | §4.4.2 | "More details over the implementation in draft-munoz-schc-over-dts-iot" | Add a proper informative reference | EDITORIAL | Reference formatting |
| 23 | Appendix A | "This becomes an Appendix (REPLACE)" | Replace placeholder | EDITORIAL | Placeholder text |

## Needed modifications to SCHC Architecture -06

| # | Architecture section | Architecture concept | Current text or definition | Proposed change | Category (ARCHITECTURE GAP / OPTIONAL CLARIFICATION) | Rationale |
|---|---|---|---|---|---|---|
| 1 | §3 Terminology — Stratum | Stratum / unit admitted to an Instance | "A background concept that identifies a portion of the network protocol stack targeted by SCHC, i.e., the contiguous layers within which SCHC processing can be applied. The Stratum defines the scope of the protocol headers that the SCHC Rules in the associated Context can address." | Append: the Stratum may extend up to and include the application layer; in that case the unit admitted to an Instance is an application-layer data unit (e.g., an object), and SCHC F/R can provide segmented, delay-tolerant transport of that unit, using protocol mechanisms defined by [RFC8724] and its extensions | ARCHITECTURE GAP (Trivial — additive clarification) | The draft's central mechanism (SCHC as object transport) is only implicitly expressible; -06's packet-oriented wording leaves the naturalness in doubt |
| 2 | §4.2.3 Session | Intermediary Endpoints / proxy deployments | Session section describes Sessions among Instances of a Domain; no intermediate Endpoint is mentioned anywhere in -06 | Append a paragraph: an Endpoint may be deployed on an intermediate node, its Instances participating in distinct Sessions toward each side, all sharing the same Context (same Domain); a profile may define proxy behavior (e.g., local acknowledgments) on that basis; this document does not define such procedures | ARCHITECTURE GAP (Trivial — explicit statement of an already-implied relationship) | The draft's SCHC Proxy (§4.1, §4.4.1) needs this deployment shape to be stated, not merely permitted by silence |
| 3 | §6 Operational considerations | Long-disruption operation | §6 covers error handling and Context consistency only | Optionally note that in disruption-prone deployments F/R timers may span days to months and Session state (SoV) must persist across connectivity gaps | OPTIONAL CLARIFICATION | Useful generally; NOT required — profiles can already set arbitrary timer values, so this does not affect the adaptation verdict |

## Final migration assessment
- Can the draft be migrated without changing technical behavior? **Mostly** — the sole technical-text change is §4.4.1.1 Phase 2 (ACK destination), which aligns the text with the draft's own Figure 11 rather than altering intended behavior.
- Can the migration be performed mechanically? **Mostly** — a small number of identified locations (Proxy split, configuration-ID scope, session-vs-transfer wording) need one-time mapping decisions; all other edits are mechanical renames.
- Does the draft expose a SCHC Architecture -06 gap? **Yes** — a Trivial one (two additive clarifications: application-layer Stratum/object as admitted unit; intermediary/proxy Endpoints).
- Is the gap required for this draft or merely useful generally? **Required for this draft** (object transport and the SCHC Proxy are its two central mechanisms) — and also useful generally for any store-and-forward or over-the-top SCHC deployment.
- What is the single most important migration issue? Resolving the overloaded "SCHC session / connection / flow" vocabulary and recasting the SCHC Proxy as two -06 Sessions sharing one Context, without importing the proxy's reliability-delegation behavior into the meaning of the -06 Session concept.
