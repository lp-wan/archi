# Architectural alignment review: draft-ietf-schc-over-networks-prone-to-disruptions-03

Sources used (verbatim, provided text; no reconstruction from prior knowledge):

- `SOURCE CONFIRMED: Static Context Header Compression and Fragmentation over networks prone to disruptions — draft-ietf-schc-over-networks-prone-to-disruptions-03 — 7 sections / 1401 lines — obtained from /Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-over-networks-prone-to-disruptions-03.txt`
- `SOURCE CONFIRMED: Static Context Header Compression (SCHC) Architecture — draft-ietf-schc-architecture-06 — 9 sections / 1681 lines — obtained from /Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt`

## Verdicts
- Conceptual equivalence: **Medium**
- Transition difficulty: **Medium**
- SCHC Architecture adaptation need: **Significant**

## Grade calibration

| Criterion | Verdict | Why not one grade higher? | Why not one grade lower? |
|---|---|---|---|
| Conceptual equivalence | Medium | The draft's core Direct-to-Satellite IoT (DtS-IoT) mechanism (Section 4.4.1) introduces a "LEO Satellite as a SCHC Proxy" that performs fragment-level local ACKs and local retransmissions without decompressing headers or performing packet reassembly. This splits the F/R state machine and directly violates the end-to-end Session model of -06. Additionally, Section 4.2.3.3 introduces "Payload Compression" to compress key-value payloads (SenML) rather than protocol headers, which goes beyond -06's header-only definition of C/D. These require substantive reframing and cannot be expressed as simple profile-specific rules or standard back-to-back Sessions, preventing a High grade. | Most other concepts in the draft map naturally to -06. The Zero Energy device topologies (Topology 1 and 4) map directly to Endpoints and Sessions; the Operator Platform maps to an Endpoint/Instance hosting a Context; the latency profiles (Hours, Day, Week, Month) map to F/R Rules with different timer/window variables; and parameters like MAX_OBJECT_SIZE and BETI map to Instance Configurations. The original technical model is not completely contradicted or unmappable, so the equivalence is not Low. |
| Transition difficulty | Medium | The migration is non-trivial and requires significant architectural judgment rather than mechanical text replacement. Specifically, the editor must decide how to recharacterize the "LEO Satellite as a SCHC Proxy" (e.g. as split F/R sessions with documented reliability risks) and how to represent the payload compression within -06's Stratum/Parser model. Several sections (Sections 3, 4.1, 4.2, 4.4) must be rewritten to separate Device/Gateway roles from physical implementations and to introduce Endpoint, Instance, Session, and Dispatcher concepts. This prevents an Easy grade. | The draft's overall structure and technical logic (timings, caching, cellular lower-layer integrations, and FEC) are stable. No fundamental redesign of the protocol logic or rewrite of the core IANA/security sections is needed, and the changes do not require a complete rewrite of the draft's cellular background material. The difficulty is not Difficult. |
| Architecture adaptation need | Significant | Under the anchor rubric, a Trivial adaptation is purely additive, and a Medium adaptation shifts the meaning of existing text without adding/removing concepts. Naturally expressing the "LEO Satellite as a SCHC Proxy" requires adding a new architectural role (a fragment-level Proxy or Split F/R Relay) and re-scoping the end-to-end Session/Instance relationship in -06. This is a significant addition of a new architectural concept and relationship, preventing a Medium grade. | The gap does not require redesigning the core concepts of Endpoint, Instance, Session, Context, or RuleID themselves (which would make it Very Significant). The existing architecture remains stable for standard end-to-end deployments, and the proxy role can be integrated as an optional extension for networks prone to disruptions. |

## Executive assessment

SCHC Architecture -06 can only partially express the concepts and technical model of [draft-ietf-schc-over-networks-prone-to-disruptions-03](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-over-networks-prone-to-disruptions-03.txt). While the basic cellular Zero Energy (ZE) topologies and LPWA caching/timer behaviors map naturally to -06, the draft introduces two key concepts that cannot be naturally expressed without architectural stretching or modification.

The principal conceptual mapping is:
- **Zero Energy Device (Dev) / DtS-IoT End-device** → [Endpoint](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L195-L199) hosting a [SCHC Instance](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L201-L205).
- **Operator Platform / SCHC Gateway** → Network [Endpoint](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L195-L199) hosting a peer [Instance](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L201-L205).
- **Latency-Mapping Profiles** → [Rules](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L207) with varying timer and window variables within the [Set of Rules (SoR)](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L212-L214) of the shared [Context](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L215-L218).
- **LEO Satellite as SCHC Proxy** → **Missing/Gap** (an intermediate node performing segment-level F/R actions without terminating the Session or reassembling the packet is not defined in -06).
- **Payload Compression (SenML)** → **Partial/Gap** (using C/D on structured application payloads rather than protocol headers).

The principal migration difficulty is recharacterizing the LEO Satellite SCHC Proxy and the payload compression within -06's end-to-end Session and header-centric terminology. Migrating the draft requires significant architectural judgment to address the split-session F/R behavior and its associated data loss risks.

An Architecture gap exists. The most significant gap is the lack of intermediate nodes or proxies that participate in the F/R state machine (sending local ACKs/retransmissions) without terminating the C/D Session or reassembling the packet. Expressing this requires a Significant adaptation of -06 to define a Proxy or Split-Session role. There is also a Trivial gap regarding payload compression, as -06's definition of C/D is strictly limited to headers.

## Native conceptual model

| Native draft concept | Meaning in the draft | Owner / logical location | Scope | Identifier or selector | Cardinality and relationships | Notes |
|---|---|---|---|---|---|---|
| Zero Energy Device (ZE Dev / Dev) / DtS-IoT End-device | Ultra-low-power small electronic circuit that harvests energy, hosts the SCHC engine, and transmits data/signaling. | Device | Device-local | Unique device identifier (e.g. device ID, MAC address) | 1 Dev participates in 1 Session per destination. | May communicate directly (Topology 1) or via helper nodes (IN/AN). |
| Intermediate Node (IN) / Assisting Node (AN) | A cellular-layer relay (e.g., UE or repeater) that transfers data and/or signaling between the Base Station and the ZE device. | Mobile network / helper device | Link-local (RAN) | Cellular UE/link identifiers | N ZE devices can use 1 IN/AN. | Operates transparently to the SCHC layer; does not execute SCHC code. |
| Operator Platform / Data Collection Platform | A gateway platform in the operator network that buffers/caches objects, exposes data to AS, and hosts the network-side SCHC entity. | Network operator | Domain/network | Onboarding identifier mapping to the SCHC flow | 1 platform serves many devices. | Exposes data via pub-sub or APIs to Application Servers. |
| LEO Satellite (as Proxy) | Satellite orbiting at LEO altitudes acting as a store-and-forward SCHC Proxy at the fragment layer. | Space segment | Segment-local | Satellite identifier | N end-devices can transit via 1 LEO sat. | Intercepts and acknowledges fragments in UL; retransmits lost fragments locally in DL. |
| SCHC Gateway (SCHC GW) | The terminating SCHC entity on the network side (e.g. at the gateway). | Network | Network-local | GW address/identity | 1 GW serves many Devices. | Reassembles and decompresses the received fragments. |
| MAX_OBJECT_SIZE | Threshold in bytes configured at the device to determine if an object is large and requires a delay-tolerant configuration. | Device context | Instance-local | Parameter | 1 value per configuration/profile. | Notified to the network using out-of-band methods. |
| Best Effort Transfer Interval (BETI) | Pacing interval (timer-based, occasion-based, or radio-event-based) after which the device transmits a set of tiles. | Device F/R engine | Instance-local | Pacing parameter | 1 value per instance. | Used to avoid access collisions on the RACH. |
| Tile Count (TC) | The number of tiles to send in each BETI. | Device F/R engine | Instance-local | Parameter | 1 value per instance (default 1). | Configured by the network. |
| Latency-mapping profiles (Hours, Day, Week, Month) | Four timer-based categories matching device recharge rates, defining retransmission and inactivity timers. | Device and Network Context | Context Rules | RuleID | N profiles map to N Rules in the SoR. | Shorter timers correspond to shorter window sizes; longer timers to larger windows. |
| Payload Compression (SenML) | Re-utilizing the SCHC engine to compress structured key-value payloads (SenML) rather than protocol headers. | Device and Network Context | Context | RuleID | 1 rule per SenML schema. | Requires FIDs like `application/senml+json.bn.1` to parsed fields. |
| Store and forward queue | Memory buffer on end-devices and LEO satellites used to store packets/fragments during revisit times. | Device / LEO Satellite | Local buffer | Queue pointers | 1 per node. | Essential for handling visibility/revisit windows in satellite networks. |
| FEC Mechanism | Forward Error Correction protecting tiles in DtS-IoT. | F/R engine on End-device and GW | Session | FEC parameters | 1 set of parameters per F/R Rule. | Described in `draft-munoz-schc-over-dts-iot`. |

## Native architectural model

Zero Energy (ZE) devices are ultra-low-power, small electronic sensors, trackers, or actuators that harvest energy from their surroundings (e.g., solar, RF). Because they often lack battery storage, or have highly constrained super-capacitors, their power source is intermittent and unreliable. This hardware reality drives a user plane design that must tolerate long delays and prioritize uplink-dominated traffic, while minimizing downlink feedback (including acknowledgments) to conserve precious energy.

To enable communication between these constrained devices and the cellular network, the draft identifies four distinct 3GPP topologies. While Topology 1 represents a direct connection between the Base Station (BS) and the ZE device, Topologies 2 and 3 introduce Intermediate Nodes (IN) and Assisting Nodes (AN)—such as repeater equipment or other UEs acting as relays. These cellular-layer relays transfer data or provide signaling/energy to excite the passive device, but they do so transparently to the SCHC layer without participating in compression or fragmentation.

To minimize overhead and network access signaling, ZE devices transmit data concurrently with network access, leveraging the Random Access Channel (RACH). This makes transmissions susceptible to collisions and link failures since they cannot be scheduled by the network. In this environment, MAC-layer feedback is inefficient; instead, the draft proposes integrating SCHC directly into the cellular lower layers, bypassing MAC-layer ARQ and relying on the SCHC fragmentation bitmap for reliability. This allows the device to power down its receiver immediately after transmitting rather than monitoring a feedback channel.

When objects exceed the transmission capacity of a single radio transport block, the draft introduces an "object transport" abstraction. Rather than fragmenting IP packets directly, the draft conceptualizes the large object as fitting an "imaginary jumbo IP package" which is then fragmented by SCHC's ACK-on-Error mechanism. Because a device may run out of energy mid-transmission, the cellular network is required to cache fragments in buffers for extended periods (e.g., hours or days) to avoid losing the energy expended in initial fragment transmissions.

The end-to-end communication is mediated by a Data Collection Platform (or Operator Platform) that acts as a proxy. The platform buffers and reassembles incoming objects and exposes them to Application Servers via APIs or IP tunnels, notifying applications using a pub-sub model. This requires devices to be onboarded onto the platform out-of-band using unique identifiers mapped to specific SCHC flows. Alternatively, in over-the-top deployments, SCHC is implemented in the application layer, and the application must handle delay tolerance and reassembly.

For Direct-to-Satellite IoT (DtS-IoT), the LPWAN gateway resides on a LEO satellite, and communication is governed by visibility windows (minutes of active line-of-sight) and revisit times (hours where the satellite is out of range). To handle the massive transfer delays caused by round-trip ACK times over satellite passes, the satellite employs a store-and-forward mechanism at the fragment layer. 

The LEO satellite acts as a SCHC Proxy: on the uplink, it intercepts fragments and locally acknowledges them to the end-device, taking responsibility for forwarding them to the Ground Station and SCHC Gateway during the next window. On the downlink, the proxy locally retransmits lost fragments to the end-device. Alternatively, the end-device and gateway protect tiles using a Forward Error Correction (FEC) mechanism to reduce reliance on feedback.

To configure these sessions, the draft defines a threshold `MAX_OBJECT_SIZE` to trigger delay-friendly profiles. It also introduces pacing parameters: a Best Effort Transfer Interval (BETI) based on timers, occasions, or radio events, and a Tile Count (TC) determining how many tiles are sent per interval to prevent access collisions. The timers are grouped into four latency-mapping profiles (Hours, Day, Week, Month) matching the device's recharging cycle, and window sizes are correlated to timer lengths. Finally, payload compression allows the SCHC C/D engine to parse and compress structured key-value payloads like SenML JSON to further minimize transmitted bits.

## Concept mapping

| Native draft concept | Meaning in the draft | SCHC -06 mapping | Match type | Scope alignment | Cardinality alignment | Semantic differences | Notes |
|---|---|---|---|---|---|---|---|
| Zero Energy Device (Dev) / End-device | Edge device hosting SCHC. | Physical host containing a [SCHC Endpoint](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L195-L199) and [SCHC Instance](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L201-L205). | Direct | Aligned. Both represent the logical termination of SCHC. | Aligned. | None. | Standard mapping. |
| Intermediate Node (IN) / Assisting Node (AN) | Cellular-layer relay. | Transparent lower-layer forwarding entity. Not mapped to a SCHC component. | Profile-specific | Aligned. Operates below the SCHC [Stratum](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L261). | Aligned. | None. | Relays do not run SCHC code. |
| Operator Platform | Network-side gateway hosting SCHC. | Host running a [SCHC Endpoint](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L195-L199) and [SCHC Instance](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L201-L205) with [Instance Manager](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L275) / [Context Repository](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L289). | Composite | Aligned. | Aligned. One platform runs N Instances for N devices. | None. | Maps to standard gateway hosting SCHC. |
| LEO Satellite (as Proxy) | Intermediate fragment-level proxy. | No natural -06 concept. -06 lacks intermediate fragment-level split-session relays. | **Missing** | **Misaligned.** -06 [Sessions](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L237-L238) are strictly end-to-end; there is no intermediate F/R role. | Misaligned. | A standard [Instance](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L201-L205) must perform full reassembly/decompression; the proxy violates this by manipulating fragments without terminating the [Session](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L237-L238). | Main architectural gap. |
| SCHC Gateway (SCHC GW) | Terminating network SCHC entity. | [SCHC Endpoint](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L195-L199) / [Instance](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L201-L205). | Direct | Aligned. | Aligned. | None. | Standard mapping. |
| MAX_OBJECT_SIZE | Size threshold for selecting rule parameters. | [Instance Configuration](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L219) (packet selection criteria/matching policy) or parameter in [Set of Variables (SoV)](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L240-L244). | Profile-specific | Aligned. Node-local matching logic. | Aligned. | None. | Used to select the appropriate RuleID. |
| Best Effort Transfer Interval (BETI) & Tile Count (TC) | F/R fragment pacing parameters. | F/R engine pacing implementation in the [Instance Configuration](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L219). | Profile-specific | Aligned. Local transmitter behavior. | Aligned. | None. | Timing behavior is outside the scope of -06. |
| Latency-mapping profiles | Recharge/timer classes. | F/R [Rules](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L207) with varying timer/window variables in the [SoR](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L212-L214) of the [Context](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L215-L218). Selected by [RuleID](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L207). | Direct | Aligned. | Aligned. | None. | Selected via RuleID. |
| Payload Compression (SenML) | Compressing structured payloads. | [C/D engine](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L292) and [Parser](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L254) operating on a [Stratum](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L261) extended to the application layer. | **Partial** | Aligned. | Aligned. | -06 defines C/D strictly for protocol headers, not structured payload data. | Secondary architectural gap. |
| Store and forward queue | Local buffers. | Local Endpoint implementation queue (outside the scope of -06). | Profile-specific | Aligned. | Aligned. | None. | Handled locally. |
| FEC Mechanism | Forward Error Correction. | F/R Mode parameters defined in the [Rule](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L207). | Profile-specific | Aligned. | Aligned. | None. | Parameterizes F/R Rules. |

## Scope and cardinality comparison

| Relationship or identifier | Draft model | Architecture -06 model | Alignment | Consequence |
|---|---|---|---|---|
| Ownership of Context | Not explicitly defined; assumed preconfigured or flashed in MCU | Shared between two or more [Instances](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L201-L205) in a [Domain](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L258), managed by a [Domain Manager](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L272-L273) | Aligned (implicit alignment) | The draft's out-of-band context provisioning maps to -06's management model |
| Ownership of Set of Rules | Pre-flashed rules or standardized firmware rules | Owned by the [Context](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L215-L218) and shared by [Instances](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L201-L205) | Aligned | None |
| Ownership of Set of Variables | Timer configurations (retransmission/inactivity) adjusted per profile | Owned by the [Session](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L237-L238); per-session runtime state | Aligned | Timer variations are session-specific parameters in the [SoV](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L240-L244) |
| Endpoint ↔ SCHC Instance | 1:1 assumed for simple devices | 1:N (an [Endpoint](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L195-L199) can host multiple logical [Instances](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L201-L205)) | Aligned (1:1 is a valid subset of 1:N) | No conflict; -06 supports more complex multi-instance devices |
| SCHC Instance ↔ Session | 1:1 assumed for devices, but split into two segments in DtS-IoT | N:M (a [Session](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L237-L238) spans two or more [Instances](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L201-L205)) | **Misaligned** (due to the proxy splitting the session F/R state machine) | The LEO satellite proxy splits the F/R state machine, which is not supported by standard end-to-end [Sessions](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L237-L238) |
| Sharing of Context between Sessions/Instances | Pre-configured or well-known rules shared by multiple devices | Permitted; multiple [Sessions](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L237-L238) can share the same [Context](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L215-L218) | Aligned | -06 naturally supports sharing standardized rules across many devices |
| RuleID scope | Context-scoped (indexes a rule in the context) | Context-scoped (indexes a [Rule](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L207) inside the SoR) | Aligned | None |
| Discriminator scope | Unique identifier associated with the SCHC flow (onboarding ID) | Used by [Dispatcher](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L245-L249) to route [Datagrams](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L267) to the correct [Instance](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L201-L205) | Aligned | The unique ID serves as the [Discriminator](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L250-L253) |
| Control Header processing scope | Not applicable (no Control Header used in the draft) | Defined before/after the [RuleID](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L207) for routing/protection | Not applicable | Compatible if a profile chooses to use it |
| Domain membership and boundaries | Devices belong to a cellular network or operator platform | Instances sharing a common set of Contexts form a [Domain](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-architecture-06.txt#L258) | Aligned | The operator platform defines the Domain boundary |

## Challenged mappings

| Concept | Initial mapping | Challenge | Final mapping | Reason for change |
|---|---|---|---|---|
| LEO Satellite as a SCHC Proxy | Composite (two back-to-back Sessions) | A standard Instance must perform full reassembly and decompression. The draft proxy stores and forwards fragments directly and generates local ACKs without reassembling the packet. This splits the F/R state machine, violating the end-to-end Session model of -06. | **Missing** | It cannot be represented using standard F/R or Sessions in -06 without a new proxy component. |
| Payload Compression (SenML) | Direct (standard C/D Rules) | -06 defines C/D strictly for protocol headers, whereas the draft compresses application payloads. Mapping it as Direct hides this conceptual shift. | **Partial** | Represents an extension of the C/D definition to application payload fields. |
| Intermediate Node (IN) / Assisting Node (AN) | Direct (Endpoint) | These nodes operate at the cellular physical/MAC layer and simply relay signals. They do not run SCHC code or parse SCHC headers. | **Profile-specific / Not Applicable** | They are transparent to the SCHC layer and do not host SCHC Endpoints. |

## Architectural risk points

- **Risk:** Split-Session Fragmentation Reliability (LEO Satellite Proxy)
  - **Why it matters:** The LEO satellite proxy intercepts fragments, stores them, and returns a local SCHC ACK to the end-device before the fragments reach the SCHC GW. If the satellite fails (e.g. burns up, experiences power loss, or suffers memory corruption) before forwarding the buffered fragments, the data is lost. However, because the end-device received a successful ACK, it will have deleted its local buffer and assumed delivery, resulting in silent data loss.
  - **Consequence for migration:** Migrating the draft requires either accepting this silent data loss risk and documenting it, or redesigning the mechanism to maintain end-to-end reliability (which increases transfer delay). Alternatively, -06 must define a new proxy role with explicit reliability semantics.

- **Risk:** RuleID and Context Synchronization in Receive Path
  - **Why it matters:** In cellular ZE networks, devices use configuration IDs to select Contexts. The draft lacks a standardized method for negotiating or synchronizing these configuration IDs. If the device and the Operator Platform's SCHC Instance become desynchronized, the receiver will apply the wrong rules, causing parsing failures or silent corruption.
  - **Consequence for migration:** The draft must explicitly define how configuration IDs map to -06 Contexts/Instance Configurations and how the Dispatcher resolves the correct active Context.

- **Risk:** Payload Compression Parser Complexity
  - **Why it matters:** Parsing and compressing key-value JSON payloads (SenML) requires a complex parser. ZE devices (especially Type A/B) are highly constrained and typically cannot afford the CPU/memory footprint of a full JSON parser and compression engine, contradicting their Zero Energy nature.
  - **Consequence for migration:** The draft must address how constrained devices can implement payload compression or recommend a binary pre-parsing step (e.g., using CBOR) before SCHC processing.

## Needed modifications to the draft under study

| # | Section / location in draft | Current concept or text | Proposed change | Category | Rationale |
|---|---|---|---|---|---|
| 1 | Section 4.4.1 | "LEO Satellite as a SCHC Proxy... locally acknowledges SCHC regular fragments... splitting the SCHC connection..." | Recharacterize the proxy as a split-session F/R relay with segment-local state, and explicitly document the reliability risk (data loss if the satellite fails after acknowledging but before forwarding). | REQUIRED FOR CONCEPTUAL ALIGNMENT | Standard SCHC does not support split-session F/R without end-to-end reassembly. The draft must align with the end-to-end assumptions of -06 or define this behavior explicitly. |
| 2 | Section 4.1 / 4.2 / 4.4 | Device (Dev) and network gateway roles (e.g., "endpoint where SCHC is terminated in the network"). | Rewrite the roles to use -06 terminology: the Device hosts a "SCHC Endpoint" and "SCHC Instance", and the network-side terminator hosts a peer "SCHC Endpoint" and "Instance" participating in a "Session". | REQUIRED FOR TERMINOLOGY MIGRATION | Align terminology with -06's Endpoint, Instance, and Session model. |
| 3 | Section 4.2.3.3 | "This section describes how the SCHC framework may be used to compress payload, in addition to the headers for which it was initially designed. ... a section of the context MUST be dedicated to the payload..." | Rephrase to describe this as extending the "Stratum" of the SCHC Instance to the application layer, using a custom payload "Parser" in the Context. | REQUIRED FOR TERMINOLOGY MIGRATION | Integrates payload compression into -06's Stratum and Parser concepts rather than using ad-hoc context divisions. |
| 4 | Section 4.2.3.1 / 4.2.3.2 | "Context provisioning... Context updating... pre-configured configurations... addressed individually with a configuration ID." | Map the configuration IDs to -06 Context Identifiers/Instance Configurations, and define how the Dispatcher uses them as Discriminators to select the active Instance. | REQUIRED FOR CONCEPTUAL ALIGNMENT | Integrates context selection into the -06 routing/dispatch architecture. |
| 5 | Section 3.1.2.1 | "unique identifier that is associated with the SCHC flow." | Clarify that the unique identifier serves as the "Discriminator" used by the "Dispatcher" on the Operator Platform. | REQUIRED FOR TERMINOLOGY MIGRATION | Align flow identifiers with -06 Dispatcher/Discriminator. |
| 6 | Section 4.2.1 | "Best Effort Transfer Interval (BETI)... paces fragment transmission." | Clarify that BETI is a pacing parameter managed locally by the F/R engine and stored in the Instance Configuration, rather than a shared Context parameter. | OPTIONAL CLARIFICATION | Clarifies that pacing is node-local configuration and does not require negotiation or shared context. |
| 7 | Section 1.1 / 1.2 | "This document normatively references [RFC5234] and has more information in 3GPPdocA and 3GPPdocB. (REPLACE)" | Remove placeholder "(REPLACE)" and update references. | EDITORIAL | Purely textual/editorial cleanup. |
| 8 | Appendix A | "This becomes an Appendix (REPLACE)" | Remove placeholder or populate Appendix A. | EDITORIAL | Purely textual/editorial cleanup. |

## Needed modifications to SCHC Architecture -06

| # | Architecture section | Architecture concept | Current text or definition | Proposed change | Category (ARCHITECTURE GAP / OPTIONAL CLARIFICATION) | Rationale |
|---|---|---|---|---|---|---|
| 1 | Section 3 | C/D (Compression/Decompression) | "C/D: SCHC function that performs the Compression and Decompression of headers." | Expand definition: "C/D: SCHC function that performs the Compression and Decompression of protocol headers and/or structured application payload fields (e.g. key-value formats) defined by a profile." | ARCHITECTURE GAP | The draft applies C/D to SenML application payloads; -06 must clarify that C/D can apply to structured payloads. |
| 2 | New Section / Section 4.2 | SCHC Proxy / Segmented Session | None (no proxy concept exists). | Introduce the concept of a "SCHC Proxy" or "Split-Session F/R Relay" where an intermediate node intercepts F/R packets, stores them, and generates local ACKs/retransmissions without terminating the C/D session or performing full packet reassembly. | ARCHITECTURE GAP | The draft's DtS-IoT model relies on a LEO satellite acting as a SCHC Proxy that splits the F/R session. |
| 3 | Section 4.2.2.4 | Dispatcher routing | "Datagrams are routed to the appropriate Instance by the Dispatcher using the Discriminator and admission rules..." | Clarify that the Discriminator can also be resolved from out-of-band management configuration or platform-level flow identifiers. | OPTIONAL CLARIFICATION | Supports the Operator Platform onboarding flow described in the draft. |

### Prose description of architectural gaps and required conceptual changes

The SCHC Architecture -06 requires significant adaptations to naturally express the technical model of [draft-ietf-schc-over-networks-prone-to-disruptions-03](file:///Users/apelov/Work/SCHC/archi/schc_drafts/draft-ietf-schc-over-networks-prone-to-disruptions-03.txt). The two primary architectural gaps are:

1. **Intermediate SCHC Proxy Role (Significant Gap):** 
   The draft's Direct-to-Satellite IoT (DtS-IoT) mechanism (Section 4.4.1) relies on a LEO Satellite acting as a "SCHC Proxy." This proxy intercepts fragmentation packets in the uplink, buffers them, and generates local SCHC ACKs back to the end-device. In the downlink, it performs local retransmissions of lost fragments. This effectively splits the F/R state machine into two hop-by-hop segments. 
   Under the current SCHC Architecture -06, a SCHC Session is strictly end-to-end between two Instances, and the F/R function is responsible for reassembling the original SCHC Packet. -06 lacks any concept of an intermediate relay, proxy, or split-session state machine that can manipulate fragments and generate feedback without fully terminating the Session or reassembling the packet. To naturally express this, -06 must be adapted to define a "SCHC Proxy" or "Split-Session F/R Relay" role, along with the rules governing fragment-level feedback delegation and the associated reliability trade-offs (e.g. the risk of silent data loss if the proxy fails after acknowledging but before forwarding).

2. **Payload Compression (Trivial Gap):**
   The draft's Section 4.2.3.3 describes "Payload Compression" where the SCHC engine is used to compress structured key-value payloads (SenML) rather than network headers. In contrast, -06's Terminology and C/D definitions are strictly limited to "headers." To naturally accommodate this, -06 must be clarified to state that the C/D engine and Parser can target structured application payload fields in addition to protocol headers, extending the "Stratum" concept to the application layer.

## Final migration assessment
- Can the draft be migrated without changing technical behavior? **Mostly**
- Can the migration be performed mechanically? **No**
- Does the draft expose a SCHC Architecture -06 gap? **Yes**
- Is the gap required for this draft or merely useful generally? **The proxy gap is required for this draft's DtS-IoT section; the payload compression gap is useful generally for structured payloads.**
- What is the single most important migration issue? **The LEO Satellite SCHC Proxy, which splits the F/R state machine (local ACKs/retransmissions) without terminating the C/D session, violating -06's end-to-end Session model and introducing data loss risks that must be explicitly resolved.**
